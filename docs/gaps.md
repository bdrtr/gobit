# Defect ledger

Every fault this repository has found in itself, with what closed it.

A row is here because the CLASS is worth keeping, not the incident: the
reproduction lives in the commit that closed it and the reasoning in the ADR the
row names. Thirty-nine files under `internal/` and `plugins/` cite these numbers
as the argument for their own code, which is why the numbers never move and a
closed row is never deleted.

**The gap inventory this file used to carry is finished**, and it left on
2026-09-08 rather than being archived: every row of groups A through G was
either built or decided with a named trigger, and where those decisions live is
[`adr/README.md`](adr/README.md). The measurements they rested on are under
[`measurements/`](measurements/).

Live rows first.

| # | Finding | Status |
|---|---|---|
| D18 | Nine columns nothing has ever written, invisible until D16's fix | **All nine closed** — the last by ADR 0055: `stock_locations.deleted_at` became `closed_at`, written by a close that is refused while the location still holds stock or a live promise. **Residue, two open questions for the owner: a location still cannot be RENAMED, and a close is FINAL — should a mis-close be undoable?** A reopen verb was drafted with the close and dropped: nothing in the invariant needs it, and clearing `closed_at` would make it the schema's one mutable stamp |
| D33 | `province` names two different things, and the address book names neither | **Closed 2026-09-08 — ADR 0067: it is the sub-country unit the tax schema already defined, and the district is not it.** The two readings had never met because the cart's tax request sends the province ALWAYS EMPTY. The end-to-end address filed an ilce as an il and is corrected; eleven hand-written declarations gained the comment none of them had, and a gate refuses a twelfth that says nothing. The DISTRICT still has no field — that is ADR 0065's trigger — and `customer_address` still has no sub-country column, which arrives when the province is first READ rather than for parity |
| D34 | A gate's godoc described a decision that was accepted and NOT BUILT, in the present tense | **Open, and open on purpose.** ADR 0030 decided on 2026-09-06 that the panel becomes a single-page client of `/admin/v1` — "every read and every write goes over the same admin API an external client would use". It is not built: `internal/adminui` renders twelve `.gohtml` templates on the server and reads through the Query layer in-process. ADR 0031 knows this and says so in the future tense ("once the panel is a client of `/admin/v1`"); `internal/arch/provider_fields_test.go` said it in the present, placing the panel's field names "on the other side of an HTTP boundary" that does not exist yet. The GATE was never wrong — its pattern reads both a `json:` tag and a map index, so it holds the spelling either way — and its godoc has been corrected rather than the gate. What stays open is the BUILD, which is a decision's implementation and not a defect; it is here because the class is: a document that describes a decided future as a present fact sends the next person to build on the shape that was decided away. Found 2026-09-09 while sizing a review section for the panel. **The build began the same day — ADR 0076**: the review screen is the panel's first served from `/admin/v1`, the session cookie widened to `/admin`, and the API's CSRF immunity became a defense rather than an absence. The row stays open because five screens are still server-rendered |
| D35 | The checkout plan's validator was weaker than the three layers it exists to duplicate | **Fixed 2026-09-09.** `checkoutPlan.validate` held the totals IDENTITY and no ceiling, so a plan with subtotal 1000, discount 3000 and total −2000 was ACCEPTED — the identity is satisfied, because −2000 really is 1000 − 3000. The cart service, the order service and two CHECK constraints all refuse that shape; this function is the one that refuses it BEFORE stock is reserved, which its own godoc gives as the reason it repeats them. **It was not a live defect and the row says so**: the validator has one production caller and `cart/discount.go` clamps a line discount to `[0, subtotal]` first, so nothing could reach it — a layer of defense weaker than the three it duplicates is the appearance of one, which is a different and smaller thing than a hole. Two ceilings added, both proved by a test that fails on the tree they were written against. The recovery path deliberately does NOT re-validate and the reason is in its godoc: there every side effect has already happened, and refusing to build the compensation chain over a total the compensations do not read would turn a recoverable execution into an unrecoverable one. Found by an adversarial pass over a property-testing design that was itself refused — the generator was proposed to re-find a defect already found by reading |
| D1 | `/paytr/callback` sat outside every guarded prefix | Fixed — ADR 0028, and the residue answered 2026-09-08: a callback is recorded in the ring's LOG and not in `audit_log` (ADR 0056) — a provider is not an actor and the table has no column for the outcome. Every outcome now leaves a line, refusals included; four were silent, all of them the class where the handler RAN. What remains is a decision, A19, and not a residue |
| D2 | `allow_backorder` published and read by nothing | Fixed — ADR 0048 |
| D3 | The address book's storefront endpoints were unauthenticated | Fixed — ADR 0043. The residue was NARROWED 2026-09-08 by ADR 0057: b2b's copy and the cart's `customer_id` both go through one published comparison, and a tree-wide gate holds it. The cart's claim was measurably an oracle for a stranger's e-mail address as well as their spending window. **Still open where no identity is bound** — those two surfaces then serve the claim, because withdrawing them from a working installation costs more than the leak; binding a verifier closes it |
| D4 | `order_exchanges.completed_at` / `canceled_at` never written | Fixed. The audit built to catch this never caught it — see D16 |
| D5 | Archiving an order left no timestamp | Fixed — migration 000007 |
| D6 | Two repository-internal transactions could not compose | Fixed, and the entry had named the wrong second module |
| D7 | OpenAPI claimed `q` searched title and handle | Fixed |
| D8 | The link's far side named an entity with no Query provider | Fixed |
| D9 | Neither order nor payment ever soft-deletes: ten `deleted_at` columns nothing writes, behind reads that all carry `deleted_at IS NULL` | Fixed — ADR 0054. Both halves answered at once: the columns are DROPPED and the deletes are not taken on. Four of the ten indexes were UNIQUE among LIVING rows, so one hand-written UPDATE reused an idempotency key — measured, and on `payments` that key is what stands between a retry and a second charge. `fulfillment` 000003's claim that "D9's ten carry no such rule" was false and is corrected. A gate refuses the columns' return |
| D10 | Nothing stopped a module's SQL naming another module's table | Fixed, and the residue closed 2026-09-08. Ownership comes from every migration set in the tree (82 tables, 25 owners) instead of the modules alone, so the ten tables the plugins and the core create are no longer owned by nobody — which under this rule meant legal for everybody. The SCANNED set is walked from the tree rather than derived from ownership: six of the ten plugins shipped no migrations on the day (seven of eleven since `ai-anthropic` joined on 2026-09-09), and deriving the population from ownership would let a plugin leave the audit by being the thing it looks for. **Widened once more the same day**: "owners plus plugins" was the same proxy one ring out, and the component sitting in it was the rig — see D13 |
| D11 | `make load-test` printed green and measured nothing | Fixed — the `-run` selector named no test |
| D12 | The panel's product list did not make the storefront's Graph call | Corrected |
| D13 | The measured catalog existed in one Docker volume and could not be rebuilt | Fixed, and the residue closed 2026-09-08. The SQL gate's population is every production PACKAGE now, not the migration owners plus the plugins: measured across 156 of them, `internal/rig` was the only component in the repository naming a table it does not own, and it was outside the walk for the same reason six plugins had been. Its crossing is exempt per TABLE rather than wholesale — the eleven it fills are listed, a twelfth is a finding, and a listed table nobody names is a finding too, which is what makes the list the exemption's own blindness floor |
| D14 | `make load-test` measured a catalog of zero products | Fixed — D11 one layer down |
| D15 | Performance figures nobody could re-check | Re-measured and corrected |
| D16 | The column audit named D4 as what it catches and never caught it | Fixed. The fix produced nine live findings — D18 |
| D17 | `CancelReturn` / `CancelClaim` had no production caller | Fixed |
| D19 | `SetPassword` read and wrote across a gap a delete could land in | Fixed. Recorded constraint rather than a residue: `LinkSalesChannel` locks the CHANNEL and then the link row, while the two delete flows each lock their own parent first. A deadlock would need a flow taking the link row BEFORE its parent, and none does — all six queries that touch `api_key_sales_channel` were enumerated on 2026-09-08 and the module's SQL is the whole population, since a second component naming that table is refused. The ordering is written down so the seventh query does not have to rediscover it |
| D20 | Three godocs called an orphaned rule "structurally impossible" | Fixed — it was measurably possible |
| D21 | A job that succeeds could not say anything | **Closed 2026-09-08 — ADR 0069.** The residue was a wall rather than an omission: the reporting call sat in `internal/core/job` and `core/plugin`, where `Job` lives, may not import an internal package. The CHANNEL is published as `core/jobreport` — three functions and no type — and the scheduler is not; the core's own three jobs were moved onto it so the tree carries ONE mechanism and not two. Both plugins that register a job ship as its consumers: `paymentpaytr` was reporting a count only to a log line, and `webhookout` carried a godoc promising a bound "the operator can see in the job's report" that the listing had no way to show |
| D22 | A plugin can be complete, tested, documented and impossible to install | Fixed — `webhookout` is in the composition root's catalog. **The row said otherwise until 2026-09-08** |
| D23 | Three audits were green on the very defect they were written for | Fixed — 89 gates read one by one, and the blindness control the round added now has its row in both READMEs' invariant tables (2026-09-08) |
| D24 | A carrier's events arrive out of order and the state machine refused all of it | Fixed |
| D25 | A handler could read a query parameter it never describes | Fixed — `TestEveryQueryParameterAHandlerReadsIsDescribed`. **The row said "not shipped" until 2026-09-08** |
| D26 | Two module api packages had no test at all and nothing asked why | Fixed |
| D27 | A data subject could be told "you are not here" by the one holder required to keep their document | Fixed — ADR 0038 |
| D28 | A guard that stops guarding at the ASCII boundary | Fixed — ADR 0038's amendment and `case_folding_test.go` |
| D29 | The documents were audited against the code and 91 statements were false | Swept once, and the four classes are now settled — **one built, one deferred with a trigger, one already closed, one that was never a gap.** ROUTE ADDRESSES: `TestTheRouteAddressesInTheProseExist`, written 2026-09-08, three defects on the day. Its exclusion of the changelog and the dated records is a DECIDED BOUNDARY with a written price and not a residue: re-measured 2026-09-08, the tree carries 508 addresses, 410 in scope, 98 in the records, and the 17 unbound among those 98 are the cost the gate names in its own godoc. The row said "393 of 491" and both numbers had rotted — the count class biting the sentence reporting it. COUNTS: `TestTheCountsInTheProseAreTrue` (ADR 0070), a closed vocabulary of eight populations the tree sizes for itself, admitted by the population's PATH on the line; six false counts on the day it shipped, in both READMEs and in both languages, plus two repaired by hand in the route gate's own godoc. CROSS-REFERENCES: **an inventory, not a closure** — twelve gates, one per reference kind the prose writes, six with a blindness control, and nothing was added. The word "closed" was withdrawn on the same round it was written: one kind is on nobody's list, a `table.column` named in a document, which no gate resolves against the schema. It is the class's stated blind spot rather than an open row — every such reference in the tree resolves today — but a closure nobody measured from the prose side is exactly the claim this defect is about. UNIVERSAL NEGATIONS: **deferred, and the trigger is named in ADR 0070** — the object of a negation is a predicate, not a name, so there is no population. ~~**Residue, for the owner: the Lines column of the measurements index is wrong in 19 rows** — a real instance of the class, deliberately ungated because it prices a document's length rather than a population; correct it or drop the column~~ **CLOSED 2026-09-09 by ADR 0075.** Neither: it was gated. The reason for leaving it out turned out to be the reason to gate it — a number that goes stale on the next commit is the one that needs something to notice — and what it really established was that the column does not belong in ADR 0070's count VOCABULARY, which is a fact about that gate's shape. Re-measured on the day: 18 of 34 rows wrong, all corrected |
| D30 | A plugin held four columns of personal data and declared none | Fixed. Every mutation proof needs `-count=1` |
| D31 | A data race in `TestCancellationActuallyStopsRemainingMigrations`, seen once | **Diagnosed and closed 2026-09-08 — ADR 0052.** golang-migrate v4.19.1 leaves the migrator's `isGracefulStop` unguarded next to an `isLocked` its mutex does guard, and the one line arming the write was gobit's `GracefulStop` send. Measured both ways: same error code, same version, same dirty flag, same regression test — the layer bought nothing. The send is gone and a gate refuses its return; the race is DORMANT rather than fixed, because the field is unexported |
| D32 | The language ratchet could not see a file until it was too late to matter | Fixed — the population is now tracked ∪ untracked-not-ignored |
| D36 | The suite could not tell the contracted cluster from one that breaks it | Fixed — ADR 0081. Measured: `internal/modules/product`, `plugins/searchpg` and `internal/modules/auth/repository` all PASSED on a `--locale=C` cluster, where none of the three expressions gobit depends on folds. Every fixture was ASCII, and each of those files gives a correct reason for being so — a module test with a non-ASCII fixture would be testing the container. What was missing was a test whose subject IS the container: three of them now, one per path the startup probe measures, kept by a gate that reads the probe rather than a list |
| D37 | A lock the race detector had never looked at, and the rule that reconciles two opposite comments was unwritten | Fixed — ADR 0082. Measured: five identical provider registries — where every payment, notification, file, fulfillment and tax plugin extends the system — each promised concurrency safety in their godoc and none had a test running them from two goroutines; deleting payment's mutex left the whole package green under `-race`. The coverage was SELECTIVE rather than absent: the core packages had one and the module extension points did not. The same measurement found that two lazy wrappers refuse `sync.Once` while ten use it, both correctly: `Bootstrap` guarantees route order, and a subscriber is not a route |
| D38 | A documented chain of commands could rot in every way except its addresses | Fixed — ADR 0083. The route gate resolves every address a document names, with its verb; a header name, a body field and a jq path are none of those. Measured: seven documents hold command blocks and exactly one CHAINS (`docs/security.md`, `$TOKEN` into `$SC` into `$PK`), which is where those unseen names become load-bearing — a renamed field makes `$TOKEN` the string `null` and every later step answers unauthorized while the page still reads correctly. The block is now executed out of the file rather than re-implemented beside it, and four mutations OF THE DOCUMENT turn it red |
| D39 | The ledger said its numbers never move and nothing checked them | Fixed — ADR 0084. Three rows were appended on 2026-09-09 carrying D33, D34 and D35, all already taken, by reading the TAIL of a file that is grouped by subject rather than sorted. Renumbered to D36-D38; `internal/arch/gap_ledger_test.go` now requires the numbers to be unique AND dense, because a skipped number reads exactly like the deleted row this file forbids |
| D40 | The category tree had no write but the soft delete, and a godoc described the one it lacked | Fixed — ADR 0085. Measured while comparing the catalog against a platform capability list: the only `UPDATE product_category` in the tree was the soft delete, the admin surface bound POST/GET/DELETE and no PATCH, and `is_active` was written by the INSERT alone — so a category switched off at creation could never be switched back on, renamed or moved. The listing's own godoc meanwhile said the admin surface "is the only way the merchant can turn a category back on", which is D29's class again. The cycle rule went into the UPDATE rather than beside it, because read-then-write lets two concurrent moves close a ring between them |
| D41 | Tax could only be added on top, and nothing recorded that as a choice | Fixed — ADR 0086. gobit computed tax one way only: the price was NET and the tax went on top, so a merchant in a market that quotes VAT-inclusive prices could not make the sticker be the amount charged, and no ADR said the absence was deliberate. The measurement is what decided the shape: extracting the net and re-taxing it with the ordinary calculation is wrong in **11.57%** of cases (1,200,006 gross values across six rates), always by one minor unit and always against the shopper — the two computations are not inverses |
| D42 | The prose NAMED the limit groups and only their number was checked | Fixed — ADR 0087. ADR 0070's count gate prices both READMEs' sentence about `docs/known-limits.md` — so many items in so many groups — and both numbers are counted from the file. The same sentence then names the groups and nothing read the names: a group was added, the count went from four to five, the gate turned green, and the sentence still listed four. The count and the list are updated by two different edits and only one of them had a gate. What this does NOT close is the defect that exposed it — an entry filed under the wrong heading — which is a judgment about meaning rather than a property of the text |
| D43 | A canceled shipment was reported as one that is already open | Fixed — ADR 0088. The chain was verified end to end: opening a parcel writes the order-to-shipment binding, cancelling does NOT remove it (nothing in the fulfilling flow deletes one), and a repeated idempotency key resolves to the canceled parcel because migration 000003 made the key unique over every shipment rather than the live ones and the repeated-key branch never looks at the status. The flow then answered `AlreadyOpen: true` from a link that had outlived the parcel — the caller was told goods were on their way, nothing was going to ship, and nothing said so. The module is not wrong: its contract says nothing about status, and that behavior is now pinned by an integration test rather than read out of the code |
| D44 | A response that carries an ARRAY was described as a single record | Fixed — ADR 0089. The order module's endpoint table is the strictest in the tree — every described endpoint must be in it, with the fields it takes and returns — and its vocabulary had two words: the single envelope and the paged list. The third shape, a plain envelope whose `data` is an array, had no word, so the one endpoint of that shape was entered as a single record and the document said so: `GET /admin/v1/orders/{id}/timeline` returns a list and OpenAPI declared one object. A generated client would have typed it as one record and failed on the first response. Found while describing a new unpaged listing, which is the only reason the missing word was noticed at all; the shape is now in the table and describing the timeline as a record turns it red |
| D45 | A claim to be settled with GOODS could not name a single one of them | Half closed — ADR 0089. `order_claims` accepts a type of `replace` and carries a refund amount; the goods had nowhere to live, and the order cannot hold them because its lines are immutable. So an operator could promise a replacement that no row in the database described, and `internal/workflows/returns/claim.go` refused to settle it with a message naming the SHIPPING as what was missing. **Closed in two steps on 2026-09-09.** The record half by ADR 0089: two tables, four endpoints, a ceiling counted under the order's lock. The shipping half by ADR 0090, the same day the row was written — the goods leave in a parcel, come out of the count as a movement whose reason is their own, and settle the claim by leaving. The refusal in `claim.go` stays and now means something narrower: money and goods are two different verbs, and it names the second one |
| D46 | The cycle guard was inside the statement and still lost the race | Fixed — ADR 0091. ADR 0085 put the ancestry walk INSIDE the UPDATE and said that this is what stops two concurrent reparents from closing a ring; the reasoning was one step short and the test that proved it ran ONE round. Under READ COMMITTED the two statements touch different rows, take no lock from one another, and each walks a snapshot from before the other committed — so "move A under B" and "move B under A" both see a clean tree and the ring is theirs together. Measured after CI failed on it: one round in three fails on a laptop. A reparent now takes an advisory lock on one key for the whole tree first, the guard refuses the second mover once it can see the first, and the race test runs twenty rounds so an unprotected tree loses better than 999 times in 1000 |
| D47 | Two advisory locks on one number, and the wait would have been silent | Fixed — ADR 0091's own gate. PostgreSQL's advisory locks live in ONE key space across the database, and `internal/core/job` documents the classes taken so far in a COMMENT. The category reparent lock was written as class 2, which the job scheduler has held since it was written: the two would have blocked each other on unrelated work, and nothing would have reported it — a lock that waits and then proceeds fails no test. Found by `internal/arch/advisory_lock_test.go` on the first run it ever made, which is the argument for the gate rather than for the comment: the classes are now compared by reading the CONSTANTS, and a key written into the SQL as a bare number is refused because nothing could compare it |
| D48 | A storefront's order could be reserved in a warehouse that storefront does not ship from | Fixed — ADR 0092. gobit had sales channels and warehouses and NOTHING between them: the checkout asked the inventory module which warehouses hold the units and the fulfillment module to rank them, and the channel the order came from was part of neither question. A shop running two storefronts out of two warehouses could not say so, and an order placed on one was reserved in the other whenever that one held the stock — silently, and correctly by every rule the framework knew. Found by walking the reservation step while measuring the capability against a platform's list. The binding is a core/link many-to-many (the channel is another module's record, so a foreign key is banned), the checkout narrows the candidates BEFORE the ranking, and a channel bound to nothing narrows nothing — the only reading that does not turn the upgrade into an outage |
| D49 | A table could join a retained document without joining its retention guard | Fixed — ADR 0097's own gate. Migration 000002 measured the hole one level up — "a guard on the parent alone protects the number and loses the document" — and answered it for the two tables that existed, in prose. Nothing then asked the question of a table added later. `invoice_line_taxes` arrived unguarded and what noticed was a FOREIGN KEY: the new child changed which spelling of TRUNCATE PostgreSQL refuses first, so two tests failed for a reason that had nothing to do with the missing trigger. Deleting the breakdown out from under a row would have left it printing one rate for a tax charged under several, with its stored `tax_total` unchanged, so nothing about the document would have looked wrong. The gate walks the foreign keys FROM `invoices` in the referencing direction — the same sentence 000002 uses, asked of the database — and requires a BEFORE DELETE guard, a BEFORE TRUNCATE companion and ENABLE ALWAYS on each table it reaches; it also names the tables it expects to reach, so a walk that quietly stops following the keys fails instead of passing on an empty population. `invoice_series` is out of the population and the reason is written: a document points AT a series rather than the series being part of one |
| D50 | A consumer's fake invented the other half of a contract, and a record shipped saying the chain was closed | Fixed — ADR 0102. ADR 0097 carried the per-rate tax breakdown into the invoicing flow and the invoice module and said the document now printed every rate. It did not: `interopInvoiceItem`, the surface the ORDER encodes an order into, had no `tax_components` field and never had. The reader read a key the producer never wrote — every total still added up, the document still printed the stack's base rate, and `known-limits.md` lost the entry that was the only remaining statement of the truth. What hid it was the flow's own test fake: `internal/workflows/invoicing/fakes_test.go` marshals an order shape written BY HAND in the consumer's package, and that shape had the field. A fake that says what the real producer cannot is worse than no test — it reports a green chain that does not exist. Found by an adversarial audit of the day's six records, whose whole job was to refuse them. Two tests now bind the hop: one in the order module asserting the WIRE its producer emits, and one end-to-end walking order → flow → invoice module → HTTP read with no fake in it. Both were mutation-proved by deleting the producer's line |
| D51 | Three places named a change as free, and it would not have started | Fixed — ADR 0116. ADR 0114 left an exchange that owes money open and named its trigger as "the day the order-to-payment link becomes one-to-many"; the payment module's link definition said the same thing in the imperative — "That day this becomes OneToMany and **nothing else changes**" — and the capability list carried it as a task. None of the three had been tried. `core/link` writes every definition into a durable ledger and compares an incoming one against the stored row for EQUALITY, so the edit those sentences describe is a startup conflict: the process does not begin, and the record it refuses is one it has itself been promising to accept. The schema half fails separately and worse — the DDL was `IF NOT EXISTS` throughout, so a looser declaration creates nothing and drops nothing, and the unique index built under the old cardinality survives to enforce it. With the ledger check passed and the drop missing, the second row is refused by `link_cardinality_violation: link ... has cardinality one_to_many: record "order_1" is already bound to another target` — an operator told that the constraint permits what the database just refused. The class is a PROMISE with no consumer: a sentence in a godoc naming a future edit as trivial is a claim about code nobody has run, and it ages exactly like a count. What closes this row is not the widening but the four mutations under `docs/measurements/0116-the-widening-lane.md`, because the same sentence could have been written again. The POPULATION is two, and the second was found by an independent reading after the first commit had already claimed the class closed: `internal/modules/invoice/service/links.go` carried the sentence almost verbatim down to the two paragraphs above it, so one was written from the other. It is the more dangerous copy of the two — before ADR 0116 a reader who believed it was stopped at startup, and after it the edit succeeds, dropping the `from_uniq` that is the only structural bar to two documents on one order and rotting `internal/workflows/invoicing/issue.go`'s stated reason for taking `ids[0]` without changing one behavior a test could see. Both copies now say what the edit really costs; neither cardinality moved |
| D52 | A record gained a state and the surfaces that report it did not | Fixed — ADR 0117. ADR 0114 brought the exchange's `completed` status back and gave the row a `completed_at` column, and the two places a reader learns about it stayed where they were: `exchangeDTO` published the WORD without the MOMENT — while `claimDTO`, the same shape fifteen lines down the same file, had carried both all along — and the order timeline emitted `exchange.canceled` alone while `claim.completed` sat two loops above it. Neither gate could see it. The endpoint table derives its described fields FROM the response type, so a type missing a field is described as missing it and the two agree about something untrue; the timeline's composition needs a wired query catalog and can only run end to end, which is where the missing entry hid. The prose said so too, in five places measured by search rather than by memory: the `Exchange` godoc said "there is no completion", the store interface said "there is no CompleteExchange beside it and there cannot be one today" three lines above the method, the route table said the withdrawal was the exchange's ONLY transition, the timeline's own godoc said "completion is gone from the record entirely" — under a paragraph warning that a comment claiming otherwise hides a bug twice — and the published OpenAPI description told clients "there is no completed state". The mapping was pulled out as `exchangeEntries` so the part that goes wrong is a pure function, the way `orderEntries` already was; both fixes are mutation-proved with `-count=1` |
| D53 | A collection could take less than it asked for and never collect the rest | Fixed — ADR 0118. Opening a session refused any collection whose captured total was above zero, and the remainder calculation under that flag subtracted only what live sessions reserved — it never read the captured total at all. So the flag was not a shortcut for the sum, it was the ONLY bar between a second session and a second charge, which is why it had to be blunt enough to catch a partial capture too. A partial capture is not a corner: the admin capture endpoint takes an optional amount and bounds it only from above, a provider may authorize in part with nobody choosing anything, the derived status vocabulary has carried a name for the result since it was written, and the integration suite already produces one against a real provider. After one, the remainder was unreachable by every route — the flag refused a new session, no query raises `amount`, and the module has no delete. The module contradicted its own written promise while it was there: its session test says a canceled session must not lock a collection for ever because the customer has to be able to try another payment method, and that held only while nothing had been captured. ADR 0042 had recorded the blockage three days earlier and named the calculation that would have to learn the sum. The cheapest-looking patch was the dangerous one and is written down in the measurement: a gate reading the NET capture opens a fully refunded collection for its whole amount and fails on the database constraint AFTER the provider has moved the money. Mutation-proved twice with `-count=1`; the pre-existing test could not see the change because it captures the full amount |
| D54 | A reason was written for a behavior that never shipped | Fixed — ADR 0118, prose only. The collection refund's godoc said captures are drawn oldest first, called that deliberate rather than arbitrary, and built a justification on top of it: the refundable remainder would stay concentrated on the most recent captures. `ListPaymentsByCollection` has ordered `created_at DESC, id DESC` since it was written and the planner walks that list as it comes, so the drawing has always been newest first and the reason described a behavior that never existed. The prose is corrected to the shipped behavior rather than the behavior to the prose, and the difference is that nobody measured which order is better: a provider that only refunds within a window of the capture would prefer the oldest drawn first so the remainder does not age out. Changing the order is a behavior change nobody asked for; the trigger is the first provider that reports a closed refund window |
| D55 | An order's record of the money can be changed by a route it never hears about | Fixed — ADR 0121 (the prose half: ADR 0119). `order_summaries` keeps `paid_total` and `refunded_total`, the order's copy of what the payment module collected and gave back, and only two production callers write them: the checkout flow clearing a cart and the returns flow making a refund. The payment module publishes `POST /admin/v1/payments/{id}/refunds`, whose handler refunds and returns without telling anything else, so a refund made there moves real money and this record never learns — the refunded total stays low, the outstanding figure reads as more settled than the order is, and the erasure sweep reads the same pair. Nothing could carry the change across: the payment module emits no events at all (`grep -rn 'Publish(' internal/modules/payment/` is empty, and there is no payment topic and no subscriber), which is the same absence ADR 0022 recorded when it refused a subscriber for being unbuildable. The surface meanwhile explained its own merge semantics — at-least-once, order-independent — by "a subscriber listening to the payment events", a caller that cannot exist; the mechanism is sound and the reason given for it described a feed nobody built. ~~**The prose half is closed here**~~ — **it was not, and the correction is the row's own class.** ADR 0119 fixed the sentence it was looking at and left the SAME false claim standing two paragraphs below it, in the indicative: "the caller is fed from the event bus: delivery is AT LEAST ONCE", and again on `SummaryTotalsInput` ("payment events are delivered at least once"). Both describe a feed that does not exist, and both were the reason the merge semantics were built the way they are — so the passage read as an explanation of a live mechanism rather than of a shape kept for a caller nobody has written. Found by a later reading that was measuring something else, which is the only reason it was found at all: no gate reads these sentences. Closed now, in the conditional the paragraphs belong in, and the godoc says what the flows really are. The migration that also names an event subscriber (`000001`) is NOT touched — a migration is a dated record. **The staleness half is closed by ADR 0121**, which built exactly what this row said it would need: the payment module publishes a capture and a refund, and the order module subscribes. The subscriber is what `000001` and the merge semantics were both written for, so nothing about either had to change — the caller they described simply started existing. The sentence that stood here until ADR 0121 — that the staleness was not closed and not queued, because shutting it needed the payment module to publish — was true of its own era and named its own trigger; the trigger fired |
| D56 | A withdrawn exchange still had goods on the way | Fixed — the claim's guard, applied to the record ADR 0114 gave the same capability to. `CancelClaim` has refused a withdrawal while any replacement of that claim is still open since the claim could promise goods at all, and it says why: the promise outlives the record that made it. ADR 0114 then let a replacement name an EXCHANGE as its source and be dispatched against it, and the guard stayed where it was written. So withdrawing an exchange succeeded with a live replacement pointing at it, the dispatch flow went on shipping against a record that said the matter was closed, and nothing between the two said so — the exact fault the claim's refusal exists to prevent, in the sibling record, reachable from the published admin route. The reading it needs already existed (`ListReplacementsByExchange` on the store, the same shape the claim's uses), so the fix is the guard and not a mechanism. Two tests, mutation-proved with `-count=1`: one that the withdrawal is refused and names the replacement to withdraw first, and one that withdrawing the replacement frees the exchange — the second because a guard that cannot be satisfied trades one stuck record for another, which is how an earlier round of this work produced a permanent lock. The asymmetry is D52's shape: a capability moved to a sibling record and the rule written for the first one did not follow |
| D57 | The capture route had the same silence as the refund route, and only the refund was written down | Fixed — ADR 0121, in the same commit that closed D55. That row named `POST /admin/v1/payments/{id}/refunds` as the route the order never hears about, and stopped there. `POST /admin/v1/payment-sessions/{id}/capture` is the sibling: a live admin route, on the module's published surface, that raises a collection's captured total with no flow anywhere on the path — so an order's `paid_total` went unrecorded in exactly the way its `refunded_total` did. It was found while measuring what a payment event would have to carry, by counting which of the six production writers of a collection's totals move the pair an order keeps; the answer is two, and D55 had named one. The fix is not a second mechanism: the same subscriber answers both topics, because what it does is read the collection rather than believe the event. What is worth keeping is the shape of the miss — a defect written down from the route somebody happened to be looking at, rather than from the population of routes that can produce it |
| D58 | A count in the prose outlived the list it priced, in the same commit that grew the list | Fixed — the sentences corrected and the count gate widened. ADR 0121 took `plugins/webhookout`'s forwarded topics from four to six. The slice grew, one sentence was updated, and FOUR others across the plugin's two files went on saying four: the package prose selling the plugin as "four event subscriptions", the comment above the calls ("four calls rather than a range"), the slice's own godoc ("it calls Subscribe four times ... a typo in any of the four"), and — the one that matters — a refusal that priced a repair built on `event_outbox` at "one topic in four while looking like it covered all of them". That last one had HALVED in force without anybody deciding it had: the payment module now writes to the outbox too, so the coverage is three of six. The refusal still stands, because half is still partial and the shape of the objection is unchanged, but a recorded reason whose measurement has moved is exactly what the next person builds on. Every gate was green over all five sentences, and correctly: the count gate's vocabulary is a CLOSED list by design (its own head measures why "every number" is noise), and topics were not in it. The vocabulary gained the entry, sized from the slice literal through the AST rather than by grepping constants, so a commented-out entry cannot fool it. Widening the gate turned up the second half of the defect: the corrected sentence did not enter the audit at all until the number and the path were put on ONE line, because the scanner is line-anchored — measured with a mutation that stayed green, not assumed. The class is the same one D52, D55 and D56 record: a capability moves and the sentence written for the old shape does not follow. What is new here is the flavour — the stale sentence is a NUMBER, and the author who broke it was the same commit that grew the population |
| D59 | A funded exchange stayed open forever with its goods already gone | Fixed — the flow now decides on the order module's ANSWER instead of on a copy of its vocabulary. ADR 0120 gave the exchange a `funded` status and widened both closing guards for it: the Go predicate `models.Exchange.Settleable` and the CHECK `order_exchanges_completed_is_settled`. The dispatch flow closes the source with a third condition of its own — `source_status != "requested"` — which is a copy of a status vocabulary the flow does not own, and it went on excluding `funded`. So an operator could collect the difference, send the goods, and watch the record stay `funded` forever: the parcel left, the money was in, and the row said the exchange was still waiting. Nothing failed and nothing was logged, because being quiet is that guard's entire job — a source that cannot be settled is deliberately left alone rather than refused, so a dispatch that really sent the goods is never failed over a state that is correct. Every layer was green: the flow's unit tests set the one status the copy knew, the model's transition table already answered `funded -> proceed`, the migration test proved the CHECK. What no layer could see is that the flow never asked. Found by walking ADR 0120's MAIN path end to end for the first time — the e2e test written the day before had walked only the EXIT, which is gap D57's defect exactly: the case written down is the one the author was looking at, not the population of cases that exist. The repair follows the principle the very same file already states one field over, about `source_settleable`: *"a flow that re-derived it would hold a second copy — one that goes on saying yes on the day the rule changes."* The producer now answers `source_open` too, derived from the transition table itself, so a status added there cannot be missed here. Ten sentences across nine files still described the pre-0120 world — including the published OpenAPI description of the dispatch endpoint, which is a promise to integrators (ADR 0026) — and all ten are corrected in the same commit |
| D60 | A query's comment was edited and its generated copy was not, and nothing noticed | Fixed — regenerated, and the gate built in ADR 0122. `internal/modules/order/queries/spending.sql` carries a long comment explaining why the refunded amount is subtracted from the spending window, and sqlc copies it verbatim onto the generated `SumCustomerSpend` in `repository/orderdb/spending.sql.go`. ADR 0121 corrected the source — the comment had said the return flow did not exist yet — and did not run `make gen`, so the two copies of one sentence disagreed for an hour, on main, with CI green. It surfaced only because a LATER change to a different query in the same module made `make gen` rewrite both. There is no verification that committed generated code matches its sources: CI has a step asking whether go.mod is up to date and no counterpart asking whether the generated tree is. The drift is silent by construction — the generated file is the one a reader lands in from a stack trace, and it is the copy nobody edits. Measuring the class found the worse half and it is what the gate was built for: the source `.sql` is not the query the database runs, so an edit to it alone is executed by NOTHING. Multiplying the B2B spending window by a thousand in the source and not regenerating leaves `go build`, `go vet`, the unit lane and the integration lane against a real PostgreSQL all clean, because they run the old query — which is the correct one. The edit is invisible exactly while it is wrong. CI now regenerates and diffs, the way it already asks whether go.mod is up to date |
| D61 | A shop gave its goods away against money that had gone back | Fixed — ADR 0124. ADR 0120 lets an exchange take its difference and the row keeps the MOMENT it was funded, because a moment is all an order row may keep about a figure the payment module owns (ADR 0119). A moment is not a balance: the collection stays reachable by the payment module's own refund route, with no flow anywhere on that path — the same absence D55 records one module over — so the money can leave and the stamp does not move. Measured rather than feared: funding an exchange, refunding its collection through `POST /admin/v1/payments/{id}/refunds`, and dispatching the replacement answered 200 with a real fulfillment id, took the units off the shelf and marked the exchange `completed` with the collection holding nothing. Three guards stand on that path and each is right about its own question — two ask the ROW whether it may be closed, and the third asked the payment module once, at funding time. The defect is not a missing rule but a rule asked once about a thing that changes. The dispatch now asks before the stock moves, where refusing still costs nothing, and again before the source is marked settled, because that step also runs on the retry path where nothing asked. Found by chasing the question ADR 0119 left open ON PURPOSE — a record naming what it had not settled is what made the hole findable, which is the argument for writing those sentences down |
| D62 | A published API description said the opposite of what the endpoint does | Fixed — eight passages corrected, three of them in `/openapi.json`. ADR 0125 made the four storefront routes that named a customer refuse an unverified claim by default, and the sentences describing the old behavior did not follow it. Three were OpenAPI descriptions, which is a promise to integrators (ADR 0026) and the half that matters: the b2b storefront's 401 read *"An installation that has bound NO identity does not answer here — it cannot check the path claim, and ADR 0057 leaves these endpoints serving rather than withdrawing a surface that works"*, and the cart's said the same. An integrator reading the document would have coded against a status the endpoint now returns as its default. The other five were Go: two godocs arguing why `IdentityLookup` exists said the refusal was "the breaking change ADR 0057 was rewritten to avoid" — which ADR 0125 then deliberately made, with a way back — and three more described a conditional that had become unconditional. Nothing caught any of it. The count gate prices populations and the schema-name gate resolves identifiers; neither reads a sentence for its meaning, and no gate can. It was found by asking, after the fact, where else the old behavior was written down — the same question that found D59's ten sentences a day earlier, which is the argument for asking it as part of the change rather than after it. The class is D52's, D55's, D56's and D59's; what is new is the SURFACE: this one was published, so the stale sentence was not an internal note but a contract |
| D63 | A Go module in this repository could be verified by nothing, and the list that decides it could be shortened silently | Fixed — `TestTheModuleListCoversEveryModuleOnDisk`. A separate `go.mod` is a separate build unit, so `go test ./...` and `golangci-lint run ./...` walk straight past it: four lanes read a hand-kept list in the Makefile instead. The fault is not hypothetical. `examples/starter` and `examples/plugin` were on the vulnerability target's list and on nothing else, so neither was ever COMPILED by a lane or linted — and the first run of the widened lint target found a misspelling in `examples/plugin` that had been there since the file was written. `contrib/identity-session` shipped with twenty-eight tests nothing ran, for the same reason, one record earlier. Each lane already carried a floor counting what it ran, and a floor cannot close this: its population IS the list, so removing an entry removes it from the floor too and everything passes — measured on the documentation gate's own floor an hour before this row. The new gate takes its population from DISK, because a directory with a go.mod is a module whatever any list says, and it holds three directions: a module missing from the list, a list naming a path with no module, and a floor lower than the list it guards. It reads the Makefile rather than a copy of the values, since two sides of a comparison from one place prove only that the copies agree (gap D16's shape). The class is the one the language detector already closes for its own roots and the documentation scan now closes for its trees: a LIST is data, and data that decides what gets verified has to be checked against the world |
| D64 | A registration could take somebody else's passkey off their account | Fixed — `Put`'s conflict update is scoped to the same owner. `contrib/identity-passkey`'s store wrote `ON CONFLICT (credential_id) DO UPDATE SET customer_id = EXCLUDED.customer_id`, so a registration carrying a credential id that was already another customer's MOVED their row onto the registering account, answered 204, and left the original owner with one fewer way in. A credential id comes from the client — the authenticator mints it and a hostile one presents whatever it likes — so this is chosen input, not a collision to be waited for. The conflict now matches only the same owner; a foreign id falls through to the primary key and the store turns that into a named error rather than a 500. Success is no longer inferred from the absence of an error either: a conflict whose WHERE did not match updates nothing and reports nothing, which would have answered 204 having written zero rows. Found by a design review of the REMOVAL slice, which is the part worth keeping: the removal was to enforce "at least one way in" under a row lock, and reviewing it surfaced that a live INSERT path could take the same count to zero with no removal at all. An invariant enforced on one path while another can break it is gap D59's defect written twice |
| D65 | A passkey could be registered that its owner can never sign in with | Fixed — the registration ceremony requires a DISCOVERABLE credential. `beginRegistration` called `BeginRegistration(user)` with no options, so nothing asked the authenticator for a resident key, while `beginSignIn` is a discoverable ceremony and this module offers no other. An authenticator that stored no user handle therefore produced a credential the module accepted with a 204 and the library refuses at sign-in with "Client-side Discoverable Assertion was attempted with a blank User Handle" — a message this repository had already seen once, while the module was being built, and read as a test-harness problem rather than as the module's. The same call now passes the existing credentials as exclusions, because every "register another passkey first" sentence in the module means another DEVICE and without them one authenticator can satisfy it twice. The first fix was itself half wrong and the test that proved it is what showed it: the library's authenticator selector sets the LEGACY `requireResidentKey` only, so a browser reading the modern `residentKey` field would have seen no requirement at all — and the selector's first argument is the ATTACHMENT rather than the attestation preference, so "none" had gone into a field where no browser knows that value. The selection is built by hand now, both spellings travel, and the attachment is left unconstrained because a passkey on a phone and one on a security key are both passkeys |
| D66 | Two published API descriptions named a status their endpoint cannot return, in two modules | Fixed — the statuses corrected and a per-module gate that compares them. `identity-passkey` documented 400 on both ceremony-finish routes and `identity-session` documented 400 on two of its three, and every one of those answers 422: the kind is `Invalid` and `core/http` maps it to `StatusUnprocessableEntity`. This is gap D62's class on a second surface, and the gate that should have caught it is the description LOOP — it asserts that every route has a description and every description has a route, which is existence in both directions and never what the description SAYS. The new test drives each refusal and compares the status the router answers with the status whose description NAMES that code. The comparison is by code rather than by "is the status listed", and that was measured rather than chosen: `core/openapi` adds default responses to every operation, so 422, 429 and 500 are in the document whether a module wrote them or not — an assertion on the status being present passes for every route by construction and was watched passing over this very drift before it was rewritten |
| D67 | One passkey had two names on a live route, and the second one was never issued | Fixed — `decodeCredentialID` refuses an encoding that does not come back as itself. Go's `base64.RawURLEncoding` does not require the trailing bits of an unpadded group to be zero, so `b25sea` and `b25seQ` both decode to the four bytes `only` and the removal accepted either, while the listing only ever issues the canonical spelling. Nothing broke: the store keys on BYTES, so both spellings reach the same row and remove the same key. That is also why it would have stayed invisible — the defect is not in what the route does today but in what it accepts, and the cost lands on whatever is written next against the path segment. An audit line, a cache key or a per-credential rate limit keyed on the string would count two names as two subjects, and that fault would surface nowhere near this function. Found by accident and the accident is the part worth keeping: a fixture in the documented-status table used the mistyped spelling, the removal answered 409 anyway, and the test was passing for a reason its author had not chosen. A test that passes for the wrong reason is a test that will keep passing when the reason goes away — the same class as the chain test in D57's measurement that answered correctly by accident in a single-row database. ADR 0130 |
| D68 | A domain change turned the rule that protects an account's last way in into the thing that removed it | Fixed — ADR 0131. A passkey is scoped to an RP ID by the authenticator that minted it, so an installation changing `Options.RPID` leaves every existing key unusable. The rows stayed and nothing recorded which relying party they belonged to, so ADR 0130's guard — written one commit earlier, to refuse the removal that would leave an account with no way in — COUNTED them: a person holding one abandoned key and one new one was told they had two ways in, and removing the NEW one was permitted. The guard produced the lockout it exists to prevent, and it did so by working exactly as written. The server had no opinion either, and that half was measured rather than reasoned: a row registered under `example.test` signed its owner in under `moved.test` with a 204, because the only relying party check is against the CONFIGURED value and the row carried none. `rp_id` is a column now and every read and write of the module's own store is scoped by it; NULL means a row from before the column and is read as the configured party, so an upgrade logs nobody out. It could not be derived instead — the stored credential carries zero attestation bytes, measured, so the relying party is not recoverable from the value, which is why the published `Credentials` interface now states the scope as part of its contract. Found by writing ADR 0130's own open questions down as questions instead of leaving them as a feeling, which is the second time in two days that a record naming what it had not settled is what made the hole findable (D61 is the first) |
| D69 | A shop could honour a deletion request and leave behind the credentials that sign the deleted person in | Fixed — ADR 0132. `contrib/identity-session` and `contrib/identity-passkey` implemented none of ADR 0029's three data-subject capabilities while holding, between them, an e-mail address, an argon2id password hash, a customer id, a per-device credential id and four timestamps. The sweep reported neither module, so a controller answering a data subject got a report that was true about everything it mentioned and silent about the two modules holding that person's means of signing in. The audit built to catch exactly this could not see them: `plugin_personaldata_test.go` walks `plugins/`, it was written when D30 found a plugin invisible, and `contrib/` did not exist then — a separate go.mod does not make a table less personal. This is the FOURTH time a hand-kept population deciding what gets verified has been found short (the language detector's roots, the documentation scan's trees, the separate-module list, and now the personal-data roots), so the roots are checked against disk rather than trusted. Two shapes are worth keeping out of the repair. The passkey erasure is deliberately NOT scoped by relying party, unlike every other statement that module runs (ADR 0131): those answer which keys can sign somebody in, this answers what is held about her, and a row abandoned by a domain move is held about her — the scoped version leaves one row of three and reports a complete deletion. And the erasure does not route through the removal endpoint, whose last-way-in rule would refuse an erasure for exactly the person it serves; a rule written for one path and applied to another is D59's defect. A third finding is about the record rather than the code: the ADR nearly claimed the sweep's reach could not be tested from a separate module, on the reasoning that `internal/` is unreachable across a module boundary. Go's internal rule is about the import PATH, `contrib/...` shares the `github.com/bdrtr/gobit/` prefix, and a probe compiled and ran — so the end-to-end test exists instead of a sentence explaining why it could not |
| D70 | Every unit a shop wrote off stayed deducted from its stock forever | Fixed — ADR 0134. The checkout's last step confirms the reservations, which DEDUCTS the stock and consumes the reservation — so an order that exists holds deducted stock and nothing to release, and a line written off afterwards is a unit nobody will send and nobody counts as stock either. Neither `CancelOrder` nor ADR 0113's partial cancellation touched it, and the order module cannot: the units live in another module. There was nothing to subscribe to either, because `order.canceled` is a TIMELINE kind and not an event, and the only caller of `Restock` is the returns flow on physical receipt. The feature list called this "a canceled unit leaves no reservation" and that sentence named the wrong mechanism in the wrong place — measuring it found the whole-order cancellation has the same hole as the partial one, which is the more common case. The order now publishes `order.line_canceled` (outbox row inside the transaction, direct publish after it) and a new flow subscribes, asks the fulfillment module how many units a live parcel holds, and puts back the increment of `min(canceled total, bought − committed)` — so a second write-off cannot re-return what the first did and a shipped unit never comes back. It is the first flow in this repository that only LISTENS, and the first stock write that is idempotent: the bus delivers at least once while adding stock is deliberately not idempotent elsewhere, so the cancellation's id is the movement's reference and a partial unique index holds it. Measured while proving that index: dropping it does not quietly lose idempotence, it breaks every movement write loudly, because `ON CONFLICT` refuses to run without it — the two halves cannot drift apart. NOT closed and named in the record: a dispatch can still ship units that were canceled |
| D71 | The document that lists this framework's limits said nothing about the modules that supply the one thing it says it does not | Fixed — the entries it was missing, and the count claim that prices them. `docs/known-limits.md` is what a reader opens to learn what gobit does not do, and its identity section ends by telling an embedder the way to close four refusing routes is "one line of wiring: bind a verifier". Since ADR 0127 there IS one to bind, in this repository — `contrib/identity-session`, and `contrib/identity-passkey` on top of it — and the word `contrib` appeared NOWHERE in the file. So a reader following the document's own advice learned neither that the thing exists nor what it does not close: a signed cookie cannot be revoked before it expires, a stolen cookie can register its own passkey and remove the owner's, a credential store an installation binds itself may answer no data-subject capability at all, changing `Options.RPID` abandons every key already registered, and self-registration's default rate limit is per process. Every one of those is written in an ADR; none was where somebody looks for a limit. The class is D62's and D66's — a published sentence that stopped being the whole truth — with the surface changed: not a description that became WRONG but a list that stayed SHORT while the population grew, which is the same shape the language detector's roots and the personal-data audit's trees were found in (D63, D69). Found by reading the README table that prices this file, in the diff of an unrelated change. The count gate prices the entries and bites when one is removed — measured — but it reads no meaning, so it can price a bullet that says nothing true; what it defends is the list not getting shorter in silence. ADR 0127/0130/0131/0132/0133 |
| D72 | A parcel could be filled with any line and any quantity, including goods a customer had been told were canceled | Fixed — ADR 0135. `POST /admin/v1/fulfillments` takes a line identifier and a quantity per item, and `normalizeItems` is the whole validation of a parcel's contents: a blank identifier, a quantity outside a global range, and the same line twice. Nothing asked whether the line is on the order, whether the quantity is within what was sold, whether units of it are already in another parcel, or whether they were written off. ADR 0134 named this unclosed as "a dispatch can still ship units that were canceled" and the sentence was narrower than the truth — measuring the endpoint found it validates NOTHING against the order, and cancellations are one case of that. The cost also doubled with ADR 0134: since canceled units' stock goes back on the shelf, shipping them afterwards takes the same goods out twice and leaves the count short — a fix landing on one side of a pair made the other side worse. The module cannot check any of it (Principle 2.1/2.4), so it resolves the fulfilling flow by name at request time — the shape the cart module uses for pricing — and refuses an item over its line's bound or on a line the answer omits. It fails CLOSED, and that was not asserted: thirty-seven tests in the module went red the moment the guard landed because their service had no bound. A RETRY asks nothing, because the first request's units are counted as committed the instant it succeeds and re-checking would refuse the one request that must be answered with the existing shipment. Two mutations survived the first pass and both are recorded: the producer's surface was covered by nothing because every test of the bound used a FAKE order module, and a test written to refuse the returns-inclusive sum leaned on a helper called `returnedOrder` that creates no return — so its own comment said the order had one and it was false |
| D73 | Two shipped storefront coupon endpoints answered 500 to the first customer who typed a code | Fixed — ADR 0136. `POST /store/v1/carts/{id}/promotions` and its DELETE sibling resolve `api.CartPromotions` from `workflows.cart.interop`, and the registered `*cart.Interop` carried neither `ApplyPromotionCode` nor `RemovePromotionCode` — it had four methods and neither was one of them. Four mechanisms had to work as designed for this to ship: the two packages cannot import each other (Principle 2.1/2.4), `c.Provide` takes `any` so registration type-checks nothing, the resolution is lazy and its failure is cached by a `sync.Once` for the life of the process, and the module's fail-closed nil guard passed because the WRAPPER was present — what was absent was one layer deeper than the guard looks. No test saw it either: every coupon test calls the flow's method DIRECTLY, and the only tests naming the routes assert that the OpenAPI description exists. So the flow was covered, the route was described, and the path between them was driven by nothing. What kept the check from being written is a sentence in another module's godoc explaining the same problem — "the compiler never sees them together" — whose first half is true and whose conclusion does not follow: neither participant may import the other, but a THIRD package in the same Go module may import both, which is now `internal/arch/interop_pins_test.go` with thirty-seven assignments. Thirty-six compiled on the first try, so the drift was ONE pair rather than many — a class that fails rarely and invisibly, which is what makes a compile-time check cheap enough to carry. The completeness gate derives its population from the names that are both provided and resolved from outside the owning module, and on its first run it named two pairs the hand-written list had missed. This is the third wrong impossibility claim in two days and the second sitting in a record as a reason not to act |
| D74 | The verification lane failed twice in one day for a reason that was in no test, no record and no reach of the people reading it | Fixed — ADR 0138. `plugins/paymentpaytr` in the morning and `internal/workflows/checkout` in the afternoon, unrelated packages, each after sixty seconds, each taking CI red. Both reported "the postgres container could not be started", which names the container the test wanted rather than the one it was waiting for: the segment `from container` is emitted only by testcontainers' REUSE branch, and what timed out was the reaper. The mechanism is a handshake nobody in this repository knew existed — one Ryuk container is shared by every `go test` package process of a session, it stops ten seconds after its last client disconnects, its container carries `AutoRemove`, and the lookup that finds it passes `All: true`, so a process starting inside the window between EXITED and removed matches a dead record and waits sixty seconds for a port that will never be published. The retry that would have looked again is already over budget: twenty seconds of allowance, spent by the single sixty-second attempt. Thirty-eight container sites over roughly thirty packages make that window a regular event. The reaper is now off on the two jobs that start containers, because a GitHub-hosted runner is destroyed with the job and there is nothing left to reap — which makes the FIX rest on a claim about the machine, held by one word of YAML that no reader would connect to a decision recorded elsewhere. So `internal/arch/reaper_test.go` pins the premise in both directions: a job may disable the reaper only while its `runs-on` names a runner classified as ephemeral, and no Makefile, script or env file may disable it at all. The class is D63/D69/D71's inverted — not a hand-kept population that went short, but a hand-kept PREMISE that nothing checked. Also recorded: the first run was read as a flake and nothing was done; it was the second, in a different package, that made it a mechanism rather than weather |
| D75 | The one way a shop was told to resolve a canceled line under an open parcel was the way its goods disappeared | Fixed — ADR 0139. How many of a line's units belong on the shelf is `min(canceled, bought - in a live parcel)` and both sides of it move, but only one had an event. A write-off under an open parcel put back the units outside the box and correctly left the rest, because a pending parcel is one the warehouse is already picking. ADR 0135 looked at exactly that state, declined to withdraw somebody's shipment on the framework's own authority, and named the shop's resolution in the same sentence: cancel the parcel. Canceling one locked the row, called the provider, flipped the status and returned — it touched no stock and told nobody, because the fulfillment module published NO events at all, which was checked rather than assumed. So the three units that parcel held were in no parcel, owed to no customer and on no shelf; nothing failed and the only trace was a debug line written earlier, when it was still true. The module now publishes `fulfillment.canceled` (outbox row inside the cancelling transaction, direct publish after it) and the cancellation flow subscribes as its second event, releasing the difference between what is owed to the shelf now and what was owed while the parcel counted — a difference of STATES, so two parcels canceled in either order put back the same total and the flow needs no memory of what it already returned. The ledger reference is the PARCEL and the LINE rather than a cancellation id, because several write-offs can share one parcel and one parcel can release several lines. Two of ten mutations survived the first pass and they were opposite mistakes worth recording: a guard against "nothing was canceled" that no test could make fail because the arithmetic already returned zero — deleted rather than tested, since a guard nothing can break claims the formula misses a case it covers — and a clamp on a negative window that every fixture happened to satisfy, now pinned by an order whose parcels hold more than it sold. A third finding was in a test: a fixture expecting two units released was wrong about which act had already run, and it was SPLIT, because one fixture that cannot separate two rules proves whichever fires first |
| D76 | Of the two ways to open a parcel, one carried the goods and the other carried the record saying whose they were | Fixed — ADR 0140. `CommittedQuantities` sums a parcel's ITEMS and the `order_fulfillment` link says which order a parcel belongs to, and no parcel had both. The fulfilling flow opens one for a whole order and wrote the binding; the surface it opens through takes no item breakdown, and its own godoc says so. The module's admin endpoint is the only one that takes items, and it wrote no binding at all — a single writer, on the path with nothing to count. Measured with a probe against a real database rather than read: a parcel opened with three units answers `{oli: 3}` to CommittedQuantities and an EMPTY list to the link. So the term `committed` was structurally zero for every parcel that had anything to contribute, and it is the MIDDLE TERM of three decisions: ADR 0134 put back `min(canceled, bought - committed)` and therefore returned units sitting in a box, stock added for goods that then shipped; ADR 0135 bounded a parcel by `bought - canceled - committed` and therefore let a second parcel hold the units the first already held; ADR 0139 released what a canceled parcel held and found no order for it. Three records describing a subtraction that never subtracted, each one green, each one tested against fakes that bound what production did not. The module now writes the binding itself — it already OWNED the definition, and the rule that put it there is that a link belongs to the side holding the record it carries, so the owner declaring it while somebody else wrote it is how one writer came to be missing. The flow's write was removed rather than kept alongside: the same rule in two places is how it was forgotten in one. Found while trying to prove ADR 0139 end to end and discovering the scenario could not be built — no endpoint produces a parcel that is both item-carrying and bound. The class is D73's: a mechanism that compiles, tests green against fakes, and does nothing |
| D77 | The ground that proves this repository end to end had quietly stopped wiring one of the flows production wires | Fixed — ADR 0141. `internal/e2e` builds the module and flow set BY HAND, deliberately — its own documentation says the order follows internal/app's — because a ground that called the real setup would be testing the setup. The cost of that copy is that it is a copy, and it had fallen behind by exactly one: seven flows in production, six on the ground. The missing one was the cancellation flow, which is the repository's ONLY flow driven entirely by the bus — nothing resolves it and nothing calls it, so an unwired one subscribes to nothing, breaks no request and reddens no test; the stock figure is simply short. Every other flow is resolved by name somewhere and fails closed, which is why none of them could have gone missing quietly. Measured from the other direction too: counting each flow's appearances in the end-to-end suite gives a single zero in a column of seven, and it is the flow whose two faults had just been found (D75, D76). The gate now compares the flow packages the two roots IMPORT — imports rather than constructor calls, because a flow is wired by whatever name its package chose and a scan for `FromContainer` would miss one that named its entry point anything else. The scenario the wiring enabled is the first that can see ADR 0134, 0135, 0139 and 0140 at once, and it fails in two distinguishable ways: with the binding removed the shelf reads ten instead of eight (the shop crediting itself with goods about to ship), with the flow unwired it reads seven (nothing came back). Also recorded: the polling helper lied twice before it told the truth — a condition function that calls `require` is `runtime.Goexit` on a goroutine that is not the test's, and a failure message passed INTO `Eventuallyf` is evaluated before the poll begins. Both drafts printed the same wrong number for different reasons, so the first fix looked like it had not worked |
| D78 | The architecture narrative called every flow a saga, and the gate that prices its numbers agreed with it | Fixed — `docs/mimari.md` sections 5, 7 and 9, and the count vocabulary. Section 7 was titled "Workflows (sagas)" and opened by equating the two; measured, ONE of the seven packages under `internal/workflows` imports the saga engine. A reader learned that a flow has an execution record, a compensation chain and an idempotency key, and would look for a `gobit stuck` entry for six packages that have none. The shape the document never mentioned at all is the one that had just cost two defects: a flow driven entirely by the bus, which nothing resolves and nothing calls, so an unwired one is silent (D75, D76, D77). Worse than the prose was the AUDIT: the count gate's vocabulary listed `saga` as a synonym of `workflows` and priced it against the directory count, so "the seven sagas" would have been admitted and certified — the gate that polices the prose was carrying the prose's own wrong equivalence. The population is now derived from the import and the two nouns are separate entries, and the corrected sentence states the number with the path on one line so the gate audits it (mutation: "2 sagas" bites). Two more sentences in the same document were measured stale: section 5 said the proof of a resolved-by-name seam is the e2e test, which is the belief that kept the compile-time pin from being written and let two coupon endpoints ship broken (D73); and the container dictionary described `<module>.interop` as "for sagas/core" when modules resolve each other's (product reads `file.interop`, auth reads `notification.interop`). Section 9's recipe for adding an event omitted both steps the build enforces — forwarding the topic and writing the outbox/publish pair. The class is D62/D66/D71's, with a new edge: this time the stale sentence had a GATE agreeing with it |
| D79 | The integration lane failed about one run in twenty on a data race in a test fake, and the three green runs before it were luck | Fixed — a mutex on `fakeDispatchBound`, and a guarded reader beside it. The fake counts the questions the dispatch bound is asked, which is how one test proves a retry asks NOTHING, and the counter was incremented with no synchronisation while `TestTwoConcurrentCreatesProduceOneFulfillment` opens two parcels on two goroutines — both of which reach it through `refuseOverDispatch` (ADR 0135). It is a race by construction, and the lane's silence was the detector reporting only what it observes: measured at HEAD, one occurrence in twenty runs, and the mutation that removes the lock again gives two in thirty. Found because an unrelated change to the same package shifted the interleaving, which is the only reason it surfaced at all — nothing about it was new. The counter is read from the test's goroutine too, so the READER is guarded as well: the same race from the other side, and a reader-only fix would have left half of it standing. The class is D74's cost, at a worse rate: one lane failure in twenty is often enough to redden a build nobody can reproduce and rare enough to be called a flake, which is the state that teaches a team that red means nothing. The race lane does NOT cover it — that lane has no build tag, and this test carries `integration` |
| D80 | The integration lane was green on this machine because of a year-old cached image CI cannot pull at all | Fixed — `plugins/files3` now names MinIO's own registry. CI went red with "pull access denied for minio/minio, repository does not exist or may require 'docker login'", which reads like a missing tag and is not: measured from this machine, Docker Hub denies an anonymous pull of the PINNED tag and of `latest` alike, so it is the repository rather than the tag, and the same tag on `quay.io/minio/minio` serves its manifest without credentials. The pin was never the problem — what changed was access underneath it, which is the class a pin cannot defend against and is why a vendor repository differs from a Docker Official Image. Every local run in the session had passed because this machine held the Docker Hub image from twelve months ago and never had to pull; the proof that the fix is real is that the local cache carries no `quay.io/...` name, so the test HAD to pull and did. The population was checked rather than assumed: three container images in the tree, and `postgres:16-alpine` and `redis:7-alpine` both answer an anonymous pull. No pinning gate was built, deliberately — the images ARE pinned, thirty-five copies of the postgres tag agree with each other, and a gate against unpinned tags would have caught nothing here; the evidence says the risk is access changing under a pin, which nothing offline can check and CI already does. The class is the gitignored-artefact one from the verification rules: a green that rests on a local artefact is not a green |
| D81 | A limit this repository CLOSED was still published as a limit, in four places, and one of them was a document I had corrected the day before | Fixed — `docs/known-limits.md`, `docs/mimari.md` (two places), and the godocs of the region interop and the payment integration test. ADR 0136 made the cross-module METHOD SET a compile-time check: a pin file assigns every container-resolved producer to the interface its consumer declares. Both limit documents still opened with "Cross-module signatures are not checked at compile time", so a reader learning what gobit does not do learned that a drifted signature is an accepted price, while a gate refuses it at build time. Worse, `docs/mimari.md` CONTRADICTED ITSELF: section 5 was corrected in the previous commit (D78) and the section 12 table and a sentence in section 5 itself were left saying the opposite — the correction was partial and nothing said so. The tree was then swept rather than spot-fixed: twenty copies of "the compiler does not check" exist, and FIFTEEN of them are TRUE and were deliberately left alone, because their subject is the JSON SCHEMA or the VALUES crossing the boundary, which the pin does not touch — a field name inside a `json.RawMessage` is still found only by a test that runs both sides. Separating the two subjects is the whole content of this row: the signature is checked, the meaning is not, and a method that stays and starts answering zero is still nobody's compile error. Also closed: `docs/mimari.md`'s own "Known limits" section is a SUBSET of the limits document (ten rows against twenty-nine) and pointed at it nowhere, so a reader read the subset as the list; it now says which it is. The class is D62/D66/D71/D78's — a published sentence that outlived its decision — with the new edge that the stale sentence survived its own correction in a neighbouring section |
| D82 | Two acts put a line's canceled units back and each subtracted work it ASSUMED the other had already done | Fixed — ADR 0142, hours after the second act shipped. ADR 0134's write-off returns `min(canceled, bought - committed)` and ADR 0139's parcel cancellation returned the difference between that window evaluated twice. Each is right alone; the pair assumes an ORDER, because the parcel act's "what was already owed" term IS the write-off's answer — a number it does not read. Nothing enforces it: the write-off has two deliveries by design, and one whose direct publish was lost arrives through the outbox relay up to a minute later, after the operator has done exactly what ADR 0135 told them to do and canceled the parcel. Reproduced with a probe: five bought, three in a parcel, all five written off, parcel handled first — the parcel act put back 3 anticipating the write-off, the write-off then found a full window and put back 5, and the shelf was credited with EIGHT units for a cancellation of five. The two references differ, so the ledger's uniqueness had nothing to compare. Both acts now compute the same TARGET and the module brings the line's total up to it, reading what is already back under the level's lock; order stops mattering and a redelivery finds the target met. The unique index on the reference was DROPPED, which is not a loosening: it refused a second WRITE, a met target refuses a second UNIT, and the same act legitimately writes twice as its target grows. Found by READING the formula's copies for agreement rather than by a failing test — they agreed, and what the reading showed was that the two terms refer to each other. Five existing tests went red on the fix, every one because it asserted the amount ONE act moves on a fixture that implied a prior state without producing it: the same assumption the code made, written into the test |
| D83 | An administrator could enrol a second factor, prove it, and still sign in with their password alone | Fixed — ADR 0147. ADR 0143 shipped the record (a sealed TOTP secret, the two endpoints, RFC 6238 checked against its own vectors) and `HasConfirmedMFA` to answer whether anybody had proven one; measured, that method's only caller was its own test, and its godoc said so. So the whole feature from a shop's side was: enrol, scan, confirm, sign in exactly as before — the inert-mechanism class (D62, D66, D71, D78) landing on the thing that claims to protect the administration, which is worse than landing on a feature because an installation that switched it on believed it was covered. `Login` now asks before it signs a token. Two defects came out with it: the single-row shape meant a re-enrolment CLEARED the confirmation, so starting one and walking away was a way out of the second factor that needed no secret at all (the new secret waits in `pending_secret` beside the proven one now), and the table's own published remedy for a lost phone — "ask again, scan the new code" — required signing in, which is the one thing that case cannot do. The way back is an operator command, because an endpoint that removed a colleague's factor would make one stolen admin session enough to strip every other account to its password. And one of my own guards was measured redundant rather than tested: a "no key" branch in the demand broke no mutation because the secret box already refuses a keyless open, so it was deleted |
| D84 | The admin panel resolved five surfaces by name and the pin gate priced none of them, so widening one broke the panel with every compiling lane green | Fixed — the five are pinned (ADR 0147). `internal/adminui` may import no module, so it declares `Session`, `Catalog`, `ProductWriter`, `PriceWriter` and `StockAdmin` itself and resolves `auth.service`, `core.query`, `product.admin`, `pricing.admin` and `inventory.admin` from the container. Adding the second factor's code to `Service.Login` left `go build`, `go vet` and every unit lane green and would have failed at BOOT with `container_type_mismatch` — gap D73 again, one tree further out, and caught this time only because the smoke lane boots the binary. The gate that exists for exactly this could not see it: its population is DERIVED, and the derivation walks names ending in `.interop`. Measured while fixing it, the tree resolves twenty-three provided names from outside the module that owns them, nineteen in that family; the five panel names are pinned now and the remaining four groups are named in the pin file as a decision still to make, because "the" consumer interface for a core service is a choice per consumer rather than one assignment |
| D85 | The promotion module answers one calculation on two surfaces, both godocs said the shapes must stay identical, and the rule lived only in those godocs | Fixed — `internal/arch/discount_schema_test.go` compares the JSON field names of the three request shapes (ADR 0148). The cart flow posts a discount request to `service/interop.go` and an operator posts the same calculation to `POST /admin/v1/promotions/compute` in `api/admin.go`; the two structs are unexported in packages that cannot see each other, so nothing but a source-level comparison can pair them. ADR 0148 added one field — the line's membership lists — to the interop item and missed the admin item, and every lane stayed green: an operator trying a category rule in the panel would have been told it discounts nothing about a cart the shop discounts, which reads as a broken RULE and sends somebody to rewrite one that is correct. This is the class where the rule stays in PROSE while the population grows (the language detector's roots, the documentation scan's trees, the separate module list, the personal-data audit's trees) and it is closed the same way — the list of pairs is DATA, and a renamed type fails as "comparing nothing" rather than passing quietly. What the gate cannot see is whether the two sides MEAN the same thing by a name; that is what the module's own tests and the end-to-end discount proof are for |
| D86 | The gate that audits the published surface walked one tree while the published surface was that tree PLUS the facade, so the root package's names were promises nothing checked | Fixed — the inventory reads the same package list the package gate reads (ADR 0150). `publishedPackages` has always declared "." — the facade at the repository root — as published, and `TestThePublishedPackagesAreTheDeclaredOnes` audits it; the NAMES gate beside it walked `core/` alone. So `gobit.App` and its five methods were exported names ADR 0026 promises to keep until 1.0.0 with nothing recording them, and it was not theoretical: `App.InProcess` was added in the same slice and the gate stayed green. This is the population-in-prose class again (D85, the language detector's roots, the documentation scan's trees) with a twist worth keeping — the two gates sat in the same package, one field apart, and disagreed about what "published" meant. The fix derives one from the other so they cannot. A first version walked each entry RECURSIVELY and descended into `contrib/` and `examples/`, which are separate Go modules whose names this module does not promise; it reads one directory per entry now, which is what a package is |
| D87 | A gate required every amount-carrying endpoint to publish its unit and could not see a balance | Fixed — `tutarAlani` in `internal/modules/payment/api/describe_internal_test.go` matches `balance` and `*_balance` beside `amount` and `*_amount` (ADR 0152). The rule the test states is "an endpoint that carries an AMOUNT has to say the unit", and its reason is written out: `integer` alone does not tell a client developer whether 100,50 TL is 100, 101 or 10050, and only the description does. The population is DERIVED from the schema rather than from a list, which is right and is why the defect was invisible — the derivation matched a field NAME, and store credit's balance endpoint publishes a minor-unit integer called `balance`. So the new endpoint entered the document with no unit note and the gate that exists for exactly that stayed green. The class is D85/D86's neighbour rather than its copy: there the rule lived in prose while the population grew, here the population is derived and the DERIVATION is narrower than the sentence it serves — a gate can be wrong about its own subject while being right about how it finds it. Measured after widening: no other endpoint in the module changed state, so the narrow form had been costing nothing until an endpoint whose money is called something else arrived |
