# Architecture decision records

Read this table first. Descend into a file only when the one sentence is not
enough.

An ADR states a decision and what it costs. The evidence behind one lives under
[`../measurements/`](../measurements/), the argument that produced it lives in
`git log`, and the open questions live in [`../gaps.md`](../gaps.md).

Records 0001–0051 predate the shape rule in [`CLAUDE.md`](../../CLAUDE.md) and
are kept as written. Each carries a two-line **Summary:** under its title; that
is the only edit they have taken.

| # | Title | Decision | Status |
|---|---|---|---|
| [0001](0001-modul-arasi-iletisim.md) | Cross-module communication: a consumer-side interface | Every cross-module dependency goes through a narrow interface the CONSUMER declares in its own package, and the implementation is resolved from the container by name at runtime. | current |
| [0002](0002-di-container-el-yazmasi.md) | DI container: hand-written instead of a library | `core/container` is written by hand instead of taking the `samber/do` dependency. | current |
| [0003](0003-migration-iptali.md) | Migration cancellation: we own the connection | Migration cancellation works because gobit owns the connection and can close it; the layers above cooperate rather than relying on `context` alone. | first layer removed by [0052](0052-the-migration-cancellation-drops-its-graceful-layer.md) |
| [0004](0004-query-veri-erisimi.md) | How the Query layer pulls data out of the modules | The Query layer pulls data out of a module through a narrow provider interface the module registers, never by importing the module. | current |
| [0005](0005-link-semasi-migration-disinda.md) | The link schema is built at declaration time, not in migration files | The link schema is built at declaration time by `LinkService.Define`, not written into migration files. | current |
| [0006](0006-workflow-modul-erisimi.md) | How workflows reach the modules | `internal/workflows` does not import the modules either; a workflow reaches them through the same Query layer and interop surfaces every other consumer uses. | current |
| [0007](0007-sertlestirme-arizada-davranis.md) | What happens when the hardening components fail | Each hardening component behaves according to its OWN failure model; there is no uniform rule. | current |
| [0008](0008-musteri-kimligi-guven-siniri.md) | Customer identity: the framework's boundary, the embedder's responsibility | Verifying customer identity is the embedding application's job. | upheld by [0043](0043-gobit-requires-an-identity-it-still-does-not-issue.md) |
| [0009](0009-cok-kiracililik-kurulum-siniri.md) | Multi-tenancy: the boundary is the installation, not the row | Multi-tenancy is not built. | current |
| [0010](0010-depo-secim-politikasi.md) | Warehouse selection: coverage is a CONSTRAINT, preference is an ORDER | Warehouse coverage is a CONSTRAINT and warehouse preference is an ORDER; the location model lives in fulfillment's own schema. | current |
| [0011](0011-yonetim-paneli-dorduncu-agac.md) | The admin panel: a fourth tree, its own identity, the core's writer | The admin panel is a fourth tree at `internal/adminui`, with its own identity and the core's writer. | amended by [0013](0013-panel-write-surface.md) and [0030](0030-the-panel-becomes-an-admin-api-client.md) |
| [0012](0012-repository-language-and-solid.md) | English is the working language, and SOLID is enforced only where it can be measured | English is the working language, and the switch is incremental and ledgered so the debt can only shrink. | current |
| [0013](0013-panel-write-surface.md) | The panel writes through a per-module admin surface, not through interop | The panel writes through a per-module admin surface typed in PRIMITIVES, not through interop. | current |
| [0014](0014-error-reporting.md) | Failures are reported to an outside collector through a core contract, and the core decides what may leave | Failures are reported to an outside collector through a contract in the core, and the CORE decides what may leave. | current |
| [0015](0015-postgresql-cluster-contract.md) | PostgreSQL is a foundation, not a supported option, and the cluster it needs is a written contract | PostgreSQL is a foundation rather than a supported option, and the cluster gobit needs is written down as a contract. | upheld by [0045](0045-pgvector-arrives-as-an-opt-in-extension-module.md) |
| [0016](0016-operator-read-surface-for-half-done-sagas.md) | The operator's read surface over half-done sagas lives in the binary | The operator's read surface over half-done sagas is `gobit stuck`, a read-only subcommand of the binary. | current |
| [0017](0017-recovering-abandoned-sagas-from-the-record.md) | An abandoned saga's compensation is replayed FROM THE RECORD | An abandoned saga's compensation replays FROM THE RECORD, with state rebuilt from the steps' own persisted outputs. | current |
| [0018](0018-web-push-is-a-device-registry-not-a-channel.md) | Web push is a plugin-owned DEVICE REGISTRY, not a notification channel | Web push ships as a plugin that brings its own module, table, routes and transport, and registers NO notification provider. | current |
| [0019](0019-scheduled-work-is-elected-by-the-occurrence.md) | Scheduled work is elected by the OCCURRENCE, made live by the LOCK | Scheduled work is elected by the OCCURRENCE and made live by the LOCK, so two processes cannot run one occurrence twice. | current |
| [0020](0020-reconciliation-asks-the-provider-and-reports.md) | Reconciliation ASKS the provider, and only reports what it hears | Reconciliation ASKS each provider about the sessions where two ledgers can silently disagree, and reports what it hears. | current |
| [0021](0021-the-server-decides-the-shipping-price.md) | The server decides the shipping price, as it already decided the line price | The storefront names WHICH shipping option; the server decides what it costs. | current |
| [0022](0022-the-saga-records-what-was-collected.md) | The saga records on the order what was actually collected | The checkout saga reads the payment collection after the capture and records the actual amounts on the order. | current |
| [0023](0023-a-promised-event-is-part-of-the-transaction.md) | A promised event is part of the transaction that promised it | A promised event is written INSIDE the transaction that promised it, and a scheduled relay publishes it. | current |
| [0024](0024-an-invoice-number-comes-from-a-row-not-a-sequence.md) | An invoice number comes from a ROW, not a sequence | An invoice number is allocated by an UPDATE on a series ROW, inside the transaction that writes the document. | one clause narrowed by [0032](0032-an-issued-invoice-refuses-erasure-in-the-schema.md) |
| [0025](0025-gobit-is-a-library-not-a-template.md) | gobit is a LIBRARY a project imports, not a template a project copies | gobit becomes a LIBRARY a project imports, not a template it copies. | direction; the surface itself is [0026](0026-the-published-surface-is-fourteen-packages.md) |
| [0026](0026-the-published-surface-is-fourteen-packages.md) | The published surface is core/: fourteen packages, and no commerce model is among them | The published surface is `core/`: fourteen packages, and no commerce model among them. | extended by [0033](0033-erasure-is-a-sweep-that-returns-an-answer.md) and [0035](0035-the-schema-vocabulary-is-published.md) |
| [0027](0027-the-composition-root-is-a-library-not-a-binary.md) | The composition root is part of the library, and the binary is fifteen lines | The composition root moves into `internal/app` and a published facade at the module root calls it, so the binary is fifteen lines. | current |
| [0028](0028-an-inbound-callback-is-registered-not-bound.md) | An inbound provider callback is REGISTERED, never bound by the plugin | A plugin REGISTERS an inbound provider callback and the core binds it, so quota, body limit, timeout, signature check and replay window apply to every one. | open decisions 1 and 2 answered by [0056](0056-a-callback-is-recorded-in-the-log-and-not-in-the-audit-table.md) |
| [0029](0029-the-embedder-is-the-data-controller.md) | The embedder is the data controller; gobit publishes the mechanism | The embedder is the data controller; gobit publishes the MECHANISM and not the policy. | current |
| [0030](0030-the-panel-becomes-an-admin-api-client.md) | The panel becomes an admin-API client, and pays for it in one named layer | The panel becomes an ordinary client of `/admin/v1`, served as static assets from the binary and authenticating with the same cookie. | current |
| [0031](0031-the-admin-session-is-twelve-hours.md) | The admin session is a fixed twelve hours, and the trade is written down | The admin session is a fixed twelve hours and never renews; the operator sets the number and gobit publishes what each end of the range costs. | current |
| [0032](0032-an-issued-invoice-refuses-erasure-in-the-schema.md) | An issued invoice refuses erasure, and the refusal lives in the schema | An issued invoice is outside erasure, and the refusal lives in the SCHEMA rather than in Go. | current |
| [0033](0033-erasure-is-a-sweep-that-returns-an-answer.md) | Erasure is a synchronous sweep that returns an answer | An erasure is a synchronous sweep across every holder of personal data, and it returns ONE report. | current |
| [0034](0034-a-person-may-see-what-a-person-may-erase.md) | A person may see what a person may erase | A person may SEE what a person may erase: disclosure is published beside erasure and assembled by the same sweep. | current |
| [0035](0035-the-schema-vocabulary-is-published.md) | The schema vocabulary is published, and the silence gets a voice | The schema vocabulary moves out of `internal/` and becomes `core/openapi`, and `Doc.UndescribedRoutes` gives the silence a voice. | current |
| [0036](0036-a-component-name-carries-its-module.md) | A schema component name carries the module that owns it | A schema component's name is prefixed with the module that owns it, applied by the composition root. | current |
| [0037](0037-the-audit-log-gains-a-reader.md) | The audit log gains a reader, and reading it is recorded | `audit.Store` gains a reader and one admin endpoint behind a new scope, and reading the audit log is itself recorded. | current |
| [0038](0038-the-storage-form-of-an-e-mail-is-one-rule.md) | The storage form of an e-mail address is one rule, and it is folded in Go | The storage form of an e-mail address is one rule: trimmed, then lower-cased BY GO and never by the database. | current |
| [0039](0039-an-option-value-is-matched-by-a-folded-form.md) | An option value is matched by a folded form, and the fold keeps what it cannot transliterate | An option value is stored twice — as the merchant typed it, and as it is matched — and the fold is Go's, never the cluster's. | current |
| [0040](0040-in-stock-is-a-catalog-answer-over-an-inventory-fact.md) | "In stock" is a CATALOG answer computed over an INVENTORY fact | A VARIANT is in stock when inventory is unmanaged, backorder is allowed, or available quantity exceeds zero; a PRODUCT is in stock when at least one variant is. | current |
| [0041](0041-a-price-filter-compares-the-base-price-at-quantity-one.md) | A price filter compares the BASE price, in the request's currency, at quantity one | A price filter compares the BASE price, in the request's currency, at quantity tier one, with no customer-group context, evaluated at the moment of the query. | current |
| [0042](0042-a-spread-arrives-as-a-settlement-row-not-a-relaxed-equality.md) | The customer pays what the merchant receives, and a spread will arrive as a SETTLEMENT ROW rather than by relaxing that equality | The customer pays what the merchant receives, and the four layers holding that equality stay four layers. | current |
| [0043](0043-gobit-requires-an-identity-it-still-does-not-issue.md) | gobit REQUIRES a customer identity at the address book, and still does not issue one | ADR 0008 stands: gobit requires a customer identity at the address book and still does not issue one. | current |
| [0044](0044-the-sales-channel-moves-into-the-catalog-path.md) | The sales channel moves into the catalog PATH, and the publishable key becomes a gate rather than a body input | The sales channel is a PATH SEGMENT on the channel-scoped catalog reads, and the publishable key becomes a gate rather than an input to the body. | current |
| [0045](0045-pgvector-arrives-as-an-opt-in-extension-module.md) | pgvector arrives as a separate OPT-IN extension module, and the cluster contract does not move | ADR 0015 is not reopened and the extensions row keeps the value `none`. | current |
| [0046](0046-metrics-leave-by-scrape-and-otlp-keeps-the-traces.md) | Metrics leave gobit by SCRAPE, and OTLP keeps the traces | Metrics leave gobit by SCRAPE and OTLP is left carrying the traces. | current |
| [0047](0047-a-replaced-price-is-deleted.md) | A replaced price is DELETED, and what survived a replace was never a history | A replaced price is DELETED and nothing is kept, inside the transaction `ReplacePrices` already serializes. | current |
| [0048](0048-the-four-carried-flags-get-a-reader-not-a-deletion.md) | All four carried flags get a reader: the stock pair in the checkout saga, the product pair in the campaign engine | All four carried flags get a reader and none stops being published: the stock pair in the checkout saga, the product pair in the campaign engine. | current |
| [0049](0049-one-group-decides-the-price-and-the-merchant-ranks-the-groups.md) | ONE group decides the price, and the MERCHANT ranks the groups | ONE group decides the price, and the MERCHANT ranks the groups. | current |
| [0050](0050-gobit-stores-one-language-and-the-second-is-the-embedders.md) | gobit stores ONE language, and the second language is the embedder's — which gobit does not pretend it makes easy | gobit stores ONE language, and the second language is the embedder's — and gobit does not pretend it makes that easy. | current |
| [0051](0051-an-unidentified-write-is-confined-or-inert.md) | The storefront accepts a write from a party it cannot identify only when that write is CONFINED or INERT, and a GATE holds the rule rather than a paragraph | A storefront accepts a write from a party it cannot identify only when that write is CONFINED or INERT, and a GATE holds the rule rather than a paragraph. | current |
| [0052](0052-the-migration-cancellation-drops-its-graceful-layer.md) | The migration cancellation drops its graceful layer | gobit no longer sends on golang-migrate's `GracefulStop` channel; closing the connection is the whole of the cancellation. | current |
| [0053](0053-the-two-clocks-stay-and-every-moment-names-its-own.md) | The two clocks stay, and every moment names the one that stamped it | gobit does not put its timestamps on one clock; each moment a reader sees says which clock stamped it. | current |
| [0054](0054-an-order-and-a-payment-are-never-deleted.md) | An order and a payment are never deleted | The ten `deleted_at` columns of the order and payment modules are dropped: an order retires by STATUS and a money record is kept. The published money-event surface keeps two moments and does not gain an authorization one. | current |
| [0055](0055-a-location-closes-empty.md) | A stock location closes empty | A warehouse is retired by CLOSING it, refused while it holds stock or a live promise; the closed row stays readable and takes no stock. | current |
| [0056](0056-a-callback-is-recorded-in-the-log-and-not-in-the-audit-table.md) | A callback is recorded in the log, and not in `audit_log` | A callback leaves a line in the ring's log rather than a row in `audit_log`, and every outcome leaves one — the refused ones included. | current |
| [0057](0057-one-comparison-holds-the-storefront-customer-claim.md) | A storefront request that NAMES a customer proves it, through one comparison | A storefront surface that names a customer refuses a claim the bound identity contradicts, through `corehttp.ProvenCustomer`; with no identity bound, and for a cart that names nobody, nothing is refused. | current |
| [0058](0058-the-rig-grows-a-skewed-taxonomy-beside-its-uniform-one.md) | The rig grows a skewed taxonomy beside its uniform one | The catalog rig can build two small categories differing only in where their members sit in the listing, so the case that decided the product filter is reproducible by command. | current |
| [0060](0060-the-two-roles-are-the-operators-to-provision.md) | The two roles are the operator's to provision, and the binary does not change | gobit ships no second DSN and no role management; the migration/runtime split is an operator's provisioning act, and what gobit owes it is a grant list rather than a knob. | current |
| [0061](0061-a-locale-needs-a-source-before-it-needs-a-home.md) | A locale needs a SOURCE before it needs a home | Gap A11 closes as a DECISION rather than a build: gobit builds neither half of the language axis, and two gates refuse a locale arriving without a record. | current |
| [0062](0062-a-callbacks-ledger-is-the-receiving-modules-table.md) | A callback's ledger is the receiving module's table | A callback gets no ledger table of its own; its durable record is the table the receiving module already owns, and the ring's log holds the refusals no module can see. | current |
| [0063](0063-a-forwarder-is-not-a-first-subscriber.md) | A forwarder is not a first subscriber | The inventory event and the file event are not published, and the outbound-webhook plugin may not stand in as their first subscriber. | current |
| [0064](0064-a-stored-instrument-waits-for-a-provider-that-can-mint-one.md) | A stored payment instrument waits for a provider that can MINT one | gobit keeps no payment instrument, and what it waits for is a provider whose upstream mints a reusable credential. | current |
| [0065](0065-the-quote-input-waits-for-a-parcel-the-tree-can-measure.md) | The quote input waits for a parcel the tree can measure | The published shipping quote input is not widened for the district and the desi, because nothing in this tree could fill either field. | current |
| [0066](0066-a-suggestion-belongs-to-the-module-that-owns-the-row.md) | A suggestion belongs to the module that owns the row, and none is built until a proposal outlives its computation | gobit builds no suggestion store. | current |
| [0067](0067-a-province-is-the-sub-country-unit-and-not-the-district.md) | A province is the sub-country unit, and not the district | `province` means what the tax schema already defined — the unit under the country, an il in Turkey. | current |
| [0068](0068-the-ledger-explains-the-count-and-does-not-become-it.md) | The ledger explains the count, and does not become it | `inventory_movements` records every change to the physical count and `stocked_quantity` stays authoritative; a reservation is not a movement, a row carries a reason instead of an actor, and nothing deletes it. | current |
| [0069](0069-the-job-reporting-channel-is-published-and-the-scheduler-is-not.md) | The job reporting channel is published and the scheduler is not | `core/jobreport` publishes the channel a scheduled run leaves its one operator line on; the runner stays internal and the core's own jobs report through the published package, so there is one mechanism and not two. | current |
| [0070](0070-a-count-is-audited-against-a-closed-vocabulary.md) | A count is audited against a closed vocabulary, and a universal negation is not audited at all | A number the prose states for a population in a closed vocabulary must equal the number the tree computes; a universal negation gets no gate, because its object is a predicate no vocabulary can close. | current |
| [0071](0071-a-machine-proposal-is-not-a-moderation.md) | A machine's proposal is stored beside a review, and it is not a moderation | A model may write a proposal about a waiting review into four columns of its own; it moves neither the status nor the moderation moment, so the human decision keeps meaning what it says. | current |
| [0072](0072-a-model-answers-a-closed-question-and-a-job-asks-it.md) | A model answers a CLOSED question, a plugin speaks to it, and a scheduled job asks | The core publishes a classification contract with a closed label set, the model client lives in a plugin, and one job — registered only when a provider is configured — turns answers into proposals. | current |
| [0073](0073-a-proposal-outlives-the-decision.md) | A proposal outlives the decision, and the queue's filter is indexed over the QUEUE | A model's proposal is not cleared when an operator decides — it is the corpus an evaluation would need — and the listing that narrows the queue by it is served by an index bounded by the queue. | current |
| [0074](0074-the-agreement-is-counted-on-read.md) | The agreement between a model and the operators is counted on read, and carries no rate | One admin read counts, per model, how many decided reviews carry a proposal and how many of those proposals name the status the review ended in — two counts and no percentage. | current |
| [0075](0075-the-measurement-index-is-checked.md) | The measurements index is checked, and the argument for leaving it alone was the argument for gating it | Every row of the measurements index must state the report's real length and every report must have a row; eighteen of thirty-four rows were wrong. | current |
| [0076](0076-the-panels-migration-begins-with-the-review-screen.md) | ADR 0030's migration begins with the review screen, and the API's immunity becomes a DEFENSE | The panel's first screen served from `/admin/v1` is the moderation queue; the session cookie widens to `/admin` and a cookie-authenticated write must carry a same-origin header, while a bearer request is untouched. | current |
| [0077](0077-a-provider-passes-a-published-compliance-suite.md) | A provider passes a PUBLISHED compliance suite, and the in-tree ones run the same suite an embedder gets | `core/providertest` publishes the rules every provider's identity obeys and the one classification rule that holds with no upstream; a gate walks the tree and makes every provider run it. | current |
| [0078](0078-a-dependency-an-embedder-inherits-is-written-down.md) | A dependency an embedder inherits is written down, and a known vulnerability fails the build | Every direct require carries a written reason and every indirect one a line in a checked list, and `govulncheck` runs on the root and both example modules with no exemption mechanism. | current |
| [0079](0079-a-benchmark-carries-a-ceiling.md) | A benchmark carries a ceiling, and the ceiling is allocations | Every benchmark in the tree is priced by a budget whose ceiling is allocations per operation, asserted in the ordinary test lane rather than a lane of its own. | current |
| [0080](0080-a-fuzz-target-ships-with-derived-seeds.md) | A fuzz target ships with seeds, and the seeds are derived where the fuzzer cannot reach | Three fuzz targets ship with a seed floor a gate enforces, the generated lane stays out of CI, and the boundaries a fuzzer cannot reach are computed and added as seeds. | current |
| [0081](0081-every-probed-fold-has-a-witness.md) | Every fold the startup probe measures has a test that fails on the wrong cluster | The suite passed on a cluster that breaks the one hard requirement of ADR 0015; each probed fold now has a witness test whose subject is the cluster, kept by a gate that reads the probe. | current |
| [0082](0082-a-concurrency-promise-is-run.md) | A promise of concurrency safety is RUN, and Bootstrap's ordering guarantee is about routes | Every type that promises concurrency safety and holds a primitive carries a named test that runs it from two goroutines, and the ordering guarantee now says it covers routes rather than subscribers. | current |
| [0083](0083-a-documented-chain-is-executed.md) | A documented chain of requests is executed, not re-implemented | The one document that chains a variable from one request into the next is read out of the file and run as a shell script, and a gate derives the population of such blocks. | current |
| [0084](0084-the-ledger-numbers-are-checked.md) | The defect ledger's numbers are checked, and the rule it already stated is now enforced | The ledger's numbers must be unique and dense, after three rows were appended with numbers already taken. | current |
| [0085](0085-a-category-can-be-changed.md) | A category can be changed, and a move that would close a ring is refused by the statement | The category tree gains the write it never had, and the rule that a category may not be moved under its own descendant lives inside the UPDATE so two concurrent moves cannot close a ring between them. | current |
| [0086](0086-a-price-can-include-its-tax.md) | A price can include its tax, and the reverse computation is not the ordinary one run backwards | A market can quote prices with the tax inside them; the flag lives on the tax region and inherits like the provider, and the tax is extracted by its own arithmetic because reusing the ordinary one is measurably wrong. | current |
| [0087](0087-the-limit-groups-are-named-and-not-only-counted.md) | The limit groups are NAMED in the prose, so the names are checked and not only the count | The README sentence that prices the known limits also names their groups, and the English names are now compared against the file's headings in order. | current |
| [0088](0088-an-idempotency-key-outlives-its-shipment.md) | An idempotency key outlives its shipment, so a repeated key can name a canceled one | Opening a shipment with a key that resolves to a canceled parcel is refused rather than reported as already open, and the module's repeated-key behaviour is pinned by an integration test. | current |
| [0089](0089-a-claim-can-say-what-it-will-send.md) | A claim can say what it will send | A claim to be settled with goods gets a record of which lines, how many, from where and by which carrier; nothing ships yet, so the status vocabulary is the two words the code can write. | current |
| [0090](0090-a-replacement-is-sent.md) | A replacement is sent, and the ledger says nobody paid for it | The goods a claim promised leave in a parcel through a flow that recovers forward, and the reservation carries a purpose so the movement they produce is not recorded as a sale. | current |
| [0091](0091-a-reparent-takes-the-trees-lock.md) | A reparent takes the tree's lock, because a guard is not a lock | Moving a category under another serializes on an advisory lock, because two UPDATEs on different rows take none from each other and each walks a snapshot from before the other committed. | current |
| [0092](0092-a-channel-ships-from-its-own-warehouses.md) | A channel ships from its own warehouses | A sales channel can be bound to the warehouses it ships from, and the checkout reserves an order only from those; a channel bound to none is narrowed by nothing. | current |
| [0093](0093-the-badge-counts-the-channels-warehouses.md) | The badge counts the channel's warehouses | The storefront's in-stock badge is narrowed to the warehouses the request's channel ships from, through a second expansion whose breakdown never reaches the response. | current |
| [0094](0094-a-product-wears-a-tax-class.md) | A product wears a tax class | A tax class is a set of products taxed the same way, owned by the tax module and resolved from its own tables, and a rate rule may be written against the class. | current |
| [0095](0095-a-rate-can-stand-on-another.md) | A rate can stand on another | A rate may stand on another of the same region and the line is taxed by the whole stack, each component floored on its own base; selection is unchanged and the chosen rate is expanded. | current |
| [0096](0096-a-line-remembers-every-rate-that-taxed-it.md) | A line remembers every rate that taxed it | The per-rate tax breakdown travels from tax through the cart and the checkout into `order_line_taxes`, and every schema on that path changes together because two of the boundaries drop unknown fields in silence. | current |
| [0097](0097-a-document-prints-every-rate-it-charged.md) | A document prints every rate it charged | The invoicing flow carries the breakdown out of the order and the invoice module stores it in `invoice_line_taxes`, closing the limit ADR 0095 opened. | current |
| [0098](0098-the-changelog-names-every-decision-since-the-last-release.md) | The changelog names every decision since the last release | Every record dated on or after the newest released section, and not named in one, must be cited in the Unreleased section; a gate derives that population from the two documents. | current |
| [0099](0099-a-price-list-carries-the-field-a-merchant-writes-on.md) | A price list carries the field a merchant writes on | `price_list` takes the `metadata` column every merchant-authored record has; the three tables the ladder computes over do not. | current |
| [0100](0100-the-customer-sees-the-goods-and-not-the-ledger.md) | The customer sees the goods, not the ledger | The storefront gets the order timeline narrowed to the order's lifecycle and its goods; the money moments and the archiving do not cross, and the response type has no amount field. | current |
| [0101](0101-a-product-wears-a-type-and-a-tax-rule-can-name-it.md) | A product wears a type, and a tax rule can name it | The catalog gains `product_type` and a product carries one; the type reaches the tax request through the catalog read the totals path already makes. | current |
| [0102](0102-the-order-hands-the-document-every-rate-it-charged.md) | The order hands the document every rate it charged | The order's invoice surface emits `tax_components`, which ADR 0097 claimed it already did; a producer-side test and an end-to-end one bind the hop. | current |
| [0103](0103-a-rule-can-name-the-product-and-its-collection.md) | A rule can name the product and its collection | A cart line carries `product_id` and `collection_id` into the promotion engine, off the product row the round already reads; a category and a tag are lists and stay out. | current |
| [0104](0104-an-image-says-what-it-shows.md) | An image says what it shows | `product_image` gains `alt_text`, non-null and trimmed, published on the storefront and in GraphQL; an empty value is HTML's own word for a decorative image. | current |
| [0105](0105-a-credit-lowers-what-is-owed-not-what-was-sold.md) | A credit lowers what is owed, not what was sold | `order_credit_lines` lowers the outstanding amount without touching the order's total; the ceiling is the total and it is checked under the order's lock. | current |
| [0106](0106-a-claim-can-show-what-went-wrong.md) | A claim can show what went wrong | A claim can carry uploaded files as evidence, bound by the upload's id alone and resolved through the file module at read time; the same file is evidence of one claim once. | current |
| [0107](0107-two-carts-become-one.md) | Two carts become one, and the quantity is the sum | A cart can be folded into another: the lines move, a variant on both sides ends with the sum of the two quantities, and the source is emptied and deleted under two locks taken in identifier order. | current |
| [0108](0108-an-image-can-be-corrected.md) | An image can be corrected, and its address cannot | A product's images can be added, patched and removed one at a time, addressed by both identifiers; the patch reaches the alt text, the rank and the metadata, and never the address. | current |
| [0109](0109-a-cart-holds-the-coupon-the-customer-typed.md) | A cart holds the coupon the customer typed, and the order spends it | `cart_promotion_code` holds the codes, the flow refuses one the promotion module cannot use and carries the rest into the discount round, and a saga step keyed on the cart redeems what applied. | current |
| [0110](0110-a-refused-promotion-says-why.md) | A refused promotion says why, and only to the operator | The admin computation reports each candidate the elimination refused with a typed reason, read from a candidate query without the status filter; the amounts of the two paths are identical and the reason never crosses to the storefront. | current |
| [0111](0111-the-cart-carries-its-own-data-into-a-rule.md) | The cart carries its own data into a rule, under a prefix | A cart's `metadata` becomes promotion rule context under `cart.`, string values only and bounded, so an embedder can rule a promotion on a concept this framework never has to know about. | current |
| [0112](0112-a-purchase-can-earn-a-unit.md) | A purchase can earn a unit | A buyget promotion counts the units its buy rules select and discounts the cheapest units the purchase did not consume; the discount request carries the unit price and the mechanic must agree with its method. | current |
| [0113](0113-a-line-can-be-written-off-in-part.md) | A line can be written off in part | `order_line_cancellations` records units of one line that will not be delivered, under the order's lock and against a ceiling shared with returns; the total, the status and the stock are untouched. | current |
| [0114](0114-an-exchange-can-send-its-goods.md) | An exchange can send its goods | A replacement names one source, a claim or an exchange, and dispatching it settles that source; an exchange is settled only when its difference is zero, and the database holds the bound. | current |
| [0115](0115-the-shop-says-who-it-is.md) | The shop says who it is | A settings module holds one store profile and the invoicing flow reads the seller from it; the caller no longer names the issuer, and issuing before it is written is refused. | current |
| [0116](0116-a-cardinality-can-be-widened.md) | A cardinality can be widened | A declared link cardinality may move to a freer one and the declaration drops the indexes that constraint no longer needs; a narrowing, and every other change to a definition, stays a conflict. | current |
| [0117](0117-the-sales-payment-link-carries-only-the-sale.md) | The sale's payment link carries only the sale | `order_payment` stays one to one and money collected against an order for another reason gets a link name of its own, because the row carries nothing that could tell two collections apart. | current |
| [0118](0118-a-collections-gate-reads-what-is-left-to-take.md) | A collection's gate reads what is left to take | Opening a payment session asks for the amount less the captured total less what live sessions reserve, rather than whether anything was ever captured, so a partial capture's remainder can be collected. | current |
| [0119](0119-an-order-does-not-keep-a-second-copy-of-the-money.md) | An order does not keep a second copy of the money | An amount the payment module owns is asked of it at the moment it decides something rather than mirrored on an order row, so what an order records about it is the moment a question was answered and never the answer's arithmetic. | current |
| [0120](0120-an-exchange-can-take-its-difference.md) | An exchange can take its difference | A positive difference is funded by naming the payment collection it was collected into, the row keeps that identifier and the moment rather than the amount, and a funded exchange leaves 'requested' so its goods can complete it and the ordinary withdrawal refuses it. | current |
| [0121](0121-the-payment-module-says-when-money-moved.md) | The payment module says when money moved | Two topics name the collection and the moment and carry no amount, and the order module subscribes and reports the collection's cumulative totals — so a capture or a refund made through the payment module's own routes stops going unrecorded. | current |
| [0122](0122-generated-code-is-verified-by-regenerating-it.md) | Generated code is verified by regenerating it | CI runs the generators in the lint job and fails on any difference, because the source of a query is not what the database runs and an edit to it alone is executed by nothing. | current |
| [0123](0123-a-schema-name-in-live-code-resolves.md) | A schema name in live code resolves | A constraint or index named in a live comment must still be in the schema, and the audited vocabulary is every name the migrations have ever defined. | current |
| [0124](0124-the-dispatch-asks-whether-the-money-is-still-there.md) | The dispatch asks whether the money is still there | A dispatch sourced from an exchange asks the payment module whether the difference is still held, before the stock moves and again before the record is closed. | current |
| [0125](0125-serving-an-unverified-customer-claim-is-a-choice.md) | Serving an unverified customer claim is a choice | The four storefront routes that served a customer claim unchecked with no verifier bound now refuse it, and the old answer is one setting away. | current |
| [0126](0126-a-customer-identity-passes-a-published-suite.md) | A customer identity passes a published suite | `core/identitytest` publishes the rules a customer identity obeys that hold with no key, store or upstream, and a verifier trusting a stripped header declares it. | current |
| [0127](0127-a-working-identity-ships-outside-the-module.md) | A working identity ships outside the module | `contrib/identity-session` is a working customer identity with its own go.mod: a signed cookie, argon2id passwords, its own table and two storefront endpoints. | current |
| [0128](0128-passkeys-ship-in-a-module-of-their-own.md) | Passkeys ship in a module of their own | `contrib/identity-passkey` adds both WebAuthn ceremonies on top of the session identity, in a separate Go module so the library lands only in the graph that asked for it. | current |
| [0129](0129-a-signing-key-rotates-without-logging-anybody-out.md) | A signing key rotates without logging anybody out | `contrib/identity-session` accepts a list of retired keys it never signs with, so changing the signing key leaves every session in flight working. | current |
| [0130](0130-a-person-can-see-their-passkeys-and-remove-one.md) | A person can see their passkeys and remove one | `contrib/identity-passkey` lists the caller's own keys and removes one, refusing under a row lock the removal that would leave an account with no way in. | current |
| [0131](0131-a-passkey-belongs-to-one-relying-party.md) | A passkey belongs to one relying party | `contrib/identity-passkey` records the relying party id a credential was registered under and answers only for the configured one, so a domain change cannot leave an abandoned row counted as a way in. | current |
| [0132](0132-the-contrib-identity-modules-answer-a-data-subject.md) | The contrib identity modules answer a data subject | Both contrib identity modules implement erasure, declaration and disclosure, and the personal-data audit walks `contrib/` as well as `plugins/`. | current |
| [0133](0133-a-shopper-opens-their-own-account.md) | A shopper opens their own account | `contrib/identity-session` ships storefront self-registration behind a proven address, with a pending row that is not an account and a single-use link. | current |
| [0134](0134-canceled-units-go-back-on-the-shelf.md) | Canceled units go back on the shelf | The order module publishes a line cancellation and a new flow puts the unshipped units back, idempotently on the cancellation's id. | current |
| [0135](0135-a-parcel-cannot-hold-more-than-the-order-owes.md) | A parcel cannot hold more than the order owes | The fulfillment module asks a flow how many units of each line an order still owes and refuses a parcel that exceeds it. | current |
| [0136](0136-the-compiler-checks-every-interop-pair.md) | The compiler checks every interop pair | One file in `internal/arch` assigns every container-resolved producer to the interface its consumer declares, and a gate keeps that list complete. | current |
| [0137](0137-a-colleague-sets-their-own-first-password.md) | A colleague sets their own first password | An administrator invites a user, the notification module's new cross-module surface carries it, and the invited person sets their own first password. | current |
| [0138](0138-the-reaper-is-off-where-the-machine-is-thrown-away.md) | The reaper is off where the machine is thrown away | The CI jobs that start containers switch the testcontainers reaper off, because a GitHub-hosted runner is destroyed with the job and an arch gate holds that premise. | current |
| [0139](0139-a-canceled-parcel-gives-its-units-back.md) | A canceled parcel gives its units back | The fulfillment module publishes a cancellation and the flow puts back the stock the parcel was holding, as the difference between two windows. | current |
| [0140](0140-a-parcel-records-which-order-it-is-for.md) | A parcel records which order it is for | The fulfillment module writes the `order_fulfillment` binding itself, on both ways a parcel is opened. | current |
| [0141](0141-the-end-to-end-ground-wires-what-production-wires.md) | The end-to-end ground wires what production wires | A gate compares the flows `internal/app` imports with the flows `internal/e2e` imports and refuses a difference. | current |
| [0142](0142-both-acts-compute-the-same-target.md) | Both acts compute the same target | A write-off and a canceled parcel each state where a line's total on the shelf should be, and the inventory module moves the difference under its lock. | current |
| [0143](0143-an-administrator-can-hold-a-second-factor.md) | An administrator can hold a second factor | An administrator enrols a TOTP authenticator for themselves, the secret is encrypted with a key the installation supplies, and enrolment is refused without one. | current |
| [0144](0144-a-rule-can-ask-about-any-of-them.md) | A rule can ask about any of them | A ninth rule operator reads the context's value SET, and the cart sends every group a customer is in beside the ranked head. | current |
| [0145](0145-a-replacement-can-send-something-else.md) | A replacement can send something else | A replacement item may name a product variant instead of an order line, so an exchange can send a different size or colour. | current |
| [0146](0146-an-operator-can-build-a-cart.md) | An operator can build a cart | The cart's admin surface gains two writes — open a cart, add a priced line — and the line write names the sales channel it is priced against. | current |
| [0147](0147-a-second-factor-is-demanded.md) | A second factor is demanded | An administrator who proved an authenticator does not get a session token from a password alone, and a lost phone is answered by `gobit mfa-reset` rather than by an endpoint. | current |
| [0148](0148-a-rule-can-ask-what-a-product-belongs-to.md) | A rule can ask what a product belongs to | The product record publishes each product's category and tag ids, and the cart sends them per line, so a promotion rule can discount a whole category. | current |
| [0149](0149-a-carrier-can-be-asked-where-a-parcel-is.md) | A carrier can be asked where a parcel is | An optional `ShipmentTracker` capability, answered by the provider in the box, and one admin read that reports the carrier's view beside the module's own without writing anything. | current |
| [0150](0150-an-embedder-can-bring-gobit-up-in-a-test.md) | An embedder can bring gobit up in a test | The facade assembles the whole installation and returns its HTTP handler instead of listening, through the same assembly the server uses. | current |
| [0151](0151-the-catalog-says-how-long-it-may-be-reused.md) | The catalog says how long it may be reused | The channel-scoped storefront reads carry `Cache-Control` when the installation sets a TTL, `private` unless it asks for `public`, and nothing by default. | current |
