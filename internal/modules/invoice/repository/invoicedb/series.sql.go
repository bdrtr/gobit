// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: series.sql

package invoicedb

import (
	"context"
)

const createSeries = `-- name: CreateSeries :one

INSERT INTO invoice_series (id, prefix, year)
VALUES ($1, $2, $3)
RETURNING id, prefix, year, last_number, created_at, updated_at
`

type CreateSeriesParams struct {
	ID     string
	Prefix string
	Year   int32
}

// invoice_series queries.
//
// The series is the only mutable row in this module, and it is mutable in
// exactly one way: its last_number goes up by one, under a lock, in the same
// transaction as the invoice that took the number.
func (q *Queries) CreateSeries(ctx context.Context, arg CreateSeriesParams) (InvoiceSeries, error) {
	row := q.db.QueryRow(ctx, createSeries, arg.ID, arg.Prefix, arg.Year)
	var i InvoiceSeries
	err := row.Scan(
		&i.ID,
		&i.Prefix,
		&i.Year,
		&i.LastNumber,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSeries = `-- name: GetSeries :one
SELECT id, prefix, year, last_number, created_at, updated_at FROM invoice_series WHERE id = $1
`

func (q *Queries) GetSeries(ctx context.Context, id string) (InvoiceSeries, error) {
	row := q.db.QueryRow(ctx, getSeries, id)
	var i InvoiceSeries
	err := row.Scan(
		&i.ID,
		&i.Prefix,
		&i.Year,
		&i.LastNumber,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSeriesByPrefixYear = `-- name: GetSeriesByPrefixYear :one
SELECT id, prefix, year, last_number, created_at, updated_at FROM invoice_series WHERE prefix = $1 AND year = $2
`

type GetSeriesByPrefixYearParams struct {
	Prefix string
	Year   int32
}

func (q *Queries) GetSeriesByPrefixYear(ctx context.Context, arg GetSeriesByPrefixYearParams) (InvoiceSeries, error) {
	row := q.db.QueryRow(ctx, getSeriesByPrefixYear, arg.Prefix, arg.Year)
	var i InvoiceSeries
	err := row.Scan(
		&i.ID,
		&i.Prefix,
		&i.Year,
		&i.LastNumber,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listSeries = `-- name: ListSeries :many
SELECT id, prefix, year, last_number, created_at, updated_at FROM invoice_series
ORDER BY year DESC, prefix
`

func (q *Queries) ListSeries(ctx context.Context) ([]InvoiceSeries, error) {
	rows, err := q.db.Query(ctx, listSeries)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []InvoiceSeries{}
	for rows.Next() {
		var i InvoiceSeries
		if err := rows.Scan(
			&i.ID,
			&i.Prefix,
			&i.Year,
			&i.LastNumber,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const takeNextNumber = `-- name: TakeNextNumber :one
INSERT INTO invoice_series (id, prefix, year, last_number)
VALUES ($1, $2, $3, 1)
ON CONFLICT (prefix, year) DO UPDATE
SET last_number = invoice_series.last_number + 1,
    updated_at  = now()
RETURNING id, prefix, year, last_number, created_at, updated_at
`

type TakeNextNumberParams struct {
	ID     string
	Prefix string
	Year   int32
}

// TakeNextNumber opens the series if it is new and takes the next number, in
// ONE statement.
//
// # Why one statement and not create-then-advance
//
// The obvious arrangement — look for the series, create it if it is missing,
// then advance it — cannot recover from its own race. Two callers issuing the
// first document of a year both find nothing and both insert; one gets a unique
// violation, and in PostgreSQL an error inside a transaction POISONS it
// (SQLSTATE 25P02). Every statement after that fails too, so the "read the
// winner's row instead" fallback has nothing left to run in. The concurrency
// test found exactly that.
//
// ON CONFLICT never raises. The insert lands or the update runs, the row comes
// back either way, and the statement takes the row lock it needs on its way
// through.
//
// # Why this single statement is the whole concurrency control
//
// No SELECT ... FOR UPDATE is taken alongside it. An UPDATE acquires the row
// lock itself and holds it until the transaction ends, and it re-reads the row
// after acquiring it, so last_number + 1 is computed from the value the other
// transaction committed rather than from a stale read. A separate lock before
// it would lock a row this statement is about to lock anyway — protection that
// looks like protection and adds none.
//
// The gap-freeness comes from WHERE the statement runs: inside the same
// transaction as the invoice that takes the number. A rollback takes the
// increment back with it, so a failed issue leaves no hole.
func (q *Queries) TakeNextNumber(ctx context.Context, arg TakeNextNumberParams) (InvoiceSeries, error) {
	row := q.db.QueryRow(ctx, takeNextNumber, arg.ID, arg.Prefix, arg.Year)
	var i InvoiceSeries
	err := row.Scan(
		&i.ID,
		&i.Prefix,
		&i.Year,
		&i.LastNumber,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
